# Automata — контекст проекта

## Что это такое
Терминальная рабочая область AI-агента. Слева — дерево папок/чатов/терминалов, справа — активная сессия.

## Текущие модули
- `internal/tree` — виджет дерева с папками, чатами, терминалами, архивом, drag & drop, модалками.
- `internal/term` — кастомный эмулятор терминала + PTY + ANSI-парсер.
- `internal/slug` — слаг для ID сессий и префиксов профилей.
- `internal/ui` — правый контейнер с чатом, чат+план и предпросмотром папок.
- `e2e` — end-to-end тесты на основе cue-tty.
- `modules/ai-knowledge` — отдельный TUI/CLI для планов, задач, памяти (интегрируется через ContextPanel).

## Активная работа
- Исправить зависание при открытии чата.
- Обеспечить автоматический и асинхронный запуск панели планов ai-knowledge.
- Сохранять работоспособность сохранения cwd/истории терминала.
- Закрепить исправление отрисовки первой иконки в предпросмотре папок.

## Последние исправления

[2026-09-21] Проблема: при добавлении canonical paths в jobs reader была пропущена закрывающая скобка import-блока, поэтому gofmt не запускался → Решение: восстановлена скобка и перед повторной проверкой выполнены gofmt и адресные тесты.
[2026-09-21] Проблема: после удаления локального slugify KnowledgePanel сохранил один вызов при чтении Kanban, поэтому UI не компилировался → Решение: профиль Kanban берётся из уже переданного canonical `KnowledgePanel.profile`.
[2026-09-21] Проблема: после введения one-reader watcher guard старые тесты запускали новый `watchSessionCmd` после того, как `syncSessionWatchers` уже выдал pending-команду, а exit-тест ожидал consumed exit → Решение: тесты используют команды, возвращённые sync, и отдельно проверяют, что exit очищает cache и передаётся Warp; guard сохраняет единственного читателя.
[2026-09-21] Проблема: первая move regression ошибочно ожидала исчезновения sibling terminal после переноса только chat → Решение: тест проверяет сохранность sibling session и отсутствие только нового terminal target; migration ограничена выбранным элементом.
[2026-09-21] Проблема: чистый `go mod tidy` с lower-case `starframe-dev/portalis` отверг upstream go.mod, объявляющий исторический путь `github.com/Starframe/portalis` → Решение: сохранён исторический import/module path, а local replace заменён immutable remote commit replacement на `starframe-dev/portalis`.
[2026-09-22] Проблема: после canonical default familiar path UI fixture продолжал писать в legacy `~/.ai/automata/sessions`, поэтому `TestCheckFamiliarsDetectsNew` не находил файл → Решение: fixture пишет в `profiles/default/sessions`, затем UI tests повторно проходят.
[2026-09-22] Проблема: полный Go suite выявил второй fixture, который создавал default `familiars.json` в legacy `automata/sessions`, поэтому Clear не очищал canonical файл → Решение: fixture использует `paths.FamiliarsJSONLPath`, main tests снова зелёные.
[2026-09-22] Проверка: `gofmt -l .`, `go vet ./...`, `go test ./... -count=1 -p 1`, `go mod verify`, `git diff --check` и две `-trimpath -buildvcs=false` сборки прошли; SHA-256 reproducible artifacts совпал: `081cb8f61ae5abccafc1da3a89f84e0fa28e069f10a5c0dfa4f1dee6826c59c2`. Tracked `automata` пересобран тем же SHA, активные профили Projects/Keller/HumanHorizon штатно перезапущены через zellij, cuTTY smoke запуска/Help/Ctrl+C прошёл.

[2026-09-08] Проблема: обработчики mouse events синхронно дописывали каждый event в фиксированные `/tmp/context-mouse.log` и `/tmp/container-mouse.log`, создавая лишний I/O и большие журналы → Решение: удалены эти записи без изменения mouse routing, `tea.WithMouseAllMotion` и watcher error logging; существующие `/tmp`-файлы не удаляются и не переносятся.
[2026-09-09] Проблема: dispatch, schedule и review_task могли оставить familiar envelope или Task JSON после ошибки второй durable-записи → Решение: общая commit/rollback-граница пишет envelope и Task как одну операцию, удаляет residual envelope и восстанавливает Task при ошибке, а notifications/signals и scheduler bookkeeping выполняются только после commit.

[2026-09-06] Проблема: Dual review regression-тест проверял количество файлов в inbox worker, хотя review envelope доставляется в inbox reviewer, поэтому ожидал 3 вместо 2 → Решение: тест проверяет отдельный `review-session/messages` каталог; дополнительно добавлена проверка реальных newline-разделителей в familiar envelope.
[2026-09-06] Проблема: scheduler-тест предполагал порядок двух Task, созданных в одну миллисекунду, хотя файловая сортировка могла вернуть любой из них → Решение: `readTasks` использует `id` как стабильный tie-breaker, а тест проверяет лимит через фактически оставшуюся queued Task.
[2026-09-06] Проблема: Dual constraint-тесты предполагали конкретную Task при одинаковом createdAt, а scheduler отдельно возвращал причину owner-running вместо idle busy → Решение: тесты выбирают фактически scheduled Task и принимают обе безопасные причины; scheduler явно блокирует уже running owner.
[2026-09-06] Проблема: тестовый файл Dual был случайно перезаписан пустым содержимым во время добавления сценария → Решение: восстановлен полный изолированный regression suite из последней проверенной версии; перед дальнейшими edits сначала читать целевой файл и после записи запускать адресный тест.
[2026-09-06] Проблема: coordinate/priority тесты сначала содержали literal `\\n` и избыточно экранированный regex, из-за чего форматтер и Dual suite падали → Решение: ожидания переведены на реальные newline и валидный regex; familiar/coordinate tests проходят.
[2026-09-06] Проблема: constraint-тест ограничивал первый scheduler pulse `limit: 1` и иногда не доходил до проверки blocked dependency при другом tie-order → Решение: pulse использует `limit: 2`, а тест проверяет, что dependency-кандидат пропущен, независимо от порядка файлов.
[2026-09-06] Проблема: Pi отклонял Dual при совместной загрузке, потому что `extensions/dual/index.ts` и `extensions/automata/index.ts` регистрировали одинаковый `update_task` → Решение: Dual reporting переименован в `dual_update_task`, Automata `update_task` сохранён; добавлен shared-registration regression test.
[2026-09-07] Проблема: первая версия Dual subscription-файла записывала literal `\\n`, поэтому JSON не читался и idempotent subscribe не срабатывал; ask-test также сначала смотрел profile-путь среды и имел лишнее экранирование regex → Решение: записывать реальный newline, изолировать `AUTOMATA_PROFILE` в тесте и проверять regex с корректным escaping.
[2026-09-07] Реализован attention control-plane Dual: `subscribe` для Task/ask completion, `cancel` для Task и `interrupt` через существующий familiar inbox; interrupt abort-ит текущий turn на polling checkpoint, jobs/preemption остаются отдельным этапом.
[2026-09-07] Проблема: direct Node job tests ссылались на отсутствующие compiled `index.js` и `safety.js` → Решение: тест и source import используют TypeScript entrypoints `index.ts`/`safety.ts`; job и safety suites теперь проходят.
[2026-09-07] Реализована связь background job → Dual Task: `taskId` сохраняется в `job.json`, `inspect` показывает jobs, а `cancel` делегирует ownership-checked SIGTERM существующему job extension.
[2026-09-07] Проблема: lifecycle event-тесты читали JSON с буквальным `\\n` после объекта и падали на `Unexpected non-whitespace character` → Решение: писать настоящий завершающий newline; event envelope теперь JSONL-compatible и идемпотентен через exclusive file creation.
[2026-09-07] Реализованы terminal events background jobs: `job_completed`, `job_failed`, `job_cancelled` записываются в существующий familiar inbox с `jobId`/`taskId` и priority; статусы `exited`/`stopped` сохранены для совместимости.
[2026-09-07] Проблема: при добавлении checkpoint helpers случайно удалился экспорт `shouldAbortIncomingMessage`, из-за чего familiar test не импортировался → Решение: сохранить публичный helper рядом с новыми timeout/checkpoint helpers; после каждого targeted edit запускать familiar test и typecheck.
[2026-09-07] Проблема: runtime checkpoint test после добавления interrupt ожидал 2 доставленных сообщения, хотя поток содержит blocker + interrupt + второй blocker → Решение: считать все фактически доставленные envelopes и ожидать 3; interrupt и blocking проверяются раздельно.
[2026-09-07] Проблема: checkpoint fixture создавал inbox без учёта установленного `AUTOMATA_PROFILE`, поэтому familiar poller читал другой каталог → Решение: изолировать и очищать profile env в fixture перед созданием session path.
[2026-09-07] Реализованы Dual checkpoints и bounded preemption: blocker нормализуется в `blocking`, busy familiar удерживает envelope до `tool_result`/`turn_end`/`agent_end`, timeout из `DUAL_BLOCKING_TIMEOUT_MS` вызывает максимум один `ctx.abort()`, затем envelope доставляется через существующий inbox.
[2026-09-07] Проблема: TypeScript-тесты сначала запускались через `pnpm exec tsx --test` и `node --import tsx`, что давало ложные export/module ошибки или `ERR_MODULE_NOT_FOUND`; обычный `tsc` также не принимал существующие `.ts` entrypoints → Решение: запускать `.mjs` suites через `node --experimental-strip-types --test`, а `allowImportingTsExtensions` закрепить в `tsconfig.json`; теперь проходит обычный `pnpm exec tsc --noEmit`.
[2026-09-07] Реализован machine-readable `inspect_agent`: Director читает зарегистрированный familiar из существующего `familiars.json`, runtime state из `status.json`/`.idle`, связанные Dual Tasks и jobs через read-only API; неизвестные agents отклоняются без создания каталогов и без пробуждения LLM. Спека: `specs/dual-inspect-agent.md`.
[2026-09-07] Проблема: targeted edit description-массивов ask случайно вставил служебный текст и Unicode-мусор в `familiar/index.ts`, из-за чего Node получил `ERR_INVALID_TYPESCRIPT_SYNTAX` → Решение: перечитать строку, удалить точный мусор, проверить repr и повторить familiar/Dual tests до перехода дальше.
[2026-09-07] Реализован публичный Dual `ask(agent, question)`: общий executor сохраняет существующий `ask_familiar`, возвращает асинхронный `ask_id`, по умолчанию запускает read-only consult fork, а `read_only: false` явно выбирает primary lane; completion остаётся через `subscribe`/`familiar_read` и штатный inbox. Спека: `specs/dual-ask-futures.md`.
[2026-09-07] Реализован read-only `inspect_queue`: Director получает ready/blocked preview queued Tasks и deterministic причины dependency/owner/scope/familiar/idle через общий scheduler decision helper; preview не dispatch-ит, не пишет Task/mailbox и не будит LLM. Спека: `specs/dual-inspect-queue.md`.
[2026-09-07] Реализован явный read-only `inspect_task`: initiator, owner и reviewer читают одну Task и связанные jobs через существующий authorization/listJobsForTask; чужие и неизвестные запросы получают structured error до job lookup. Generic `inspect` не изменён. Спека: `specs/dual-inspect-task.md`.
[2026-09-07] Реализована structured blocked escalation: `inspect_queue` и `schedule.skipped` сохраняют прежний reason и добавляют типизированные block code/recovery/related IDs; авторизованный `message(type: "blocker", task_id)` переводит non-terminal Task в `blocked`, пишет audit и публикует blocking subscription event через существующий inbox. Unknown/unauthorized/terminal blocker не пишет mailbox; blocker без Task ID Task не мутирует. Нового daemon/transport/автоматического ask нет. Спека: `specs/dual-blocked-escalation.md`.
[2026-09-07] Реализован read-only `inspect_workspace`: по authorization Task возвращаются сохранённые path/scope/status/owner, связанные jobs через `listJobsForTask` и runtime owner через `familiars.json`/`.idle` без shell, LLM или wake-up. Unknown/unauthorized запросы получают structured error до job lookup; `inspect_diff` оставлен отдельным срезом. Спека: `specs/dual-inspect-workspace.md`.
[2026-09-07] Реализован read-only `inspect_diff`: authorization Task получает Git porcelain status workspace через mockable `execFileSync` runner без shell-интерполяции, с entries/touchedFiles/clean/outOfScopeFiles. Unknown/unauthorized/empty workspace и Git failure возвращают structured error без запуска runner либо мутаций; owner/reviewer разрешены. Спека: `specs/dual-inspect-diff.md`.
[2026-09-07] Live Pi checkpoint smoke: прямой PTY с `PI_OFFLINE=1` загрузил extensions `dual`/`familiar`/`job`; `/dual on` и `/dual off` дали ожидаемые notifications. Problem: cuetty `launch env`/`text` показал пустой экран → Solution: изолированный абсолютный Pi entrypoint и ANSI-stripped direct PTY assertion.
[2026-09-07] Реализован permission graph: familiar Dual delivery разрешает parent alias `owner`, siblings из parent `familiars.json` и direct children из собственного registry; неизвестный recipient сохраняет прежний queued mailbox contract. Hard `interrupt` от familiar отклоняется до mailbox write, Director interrupt сохраняется. Спека: `specs/dual-permission-graph.md`.
[2026-09-07] Политика recursive delegation уточнена: разрешён только один уровень Director → familiar. `summon_familiar` сохраняет `depth = 1` для direct familiar, а вызов из familiar session получает structured `familiar_cannot_spawn` без записи state; legacy entries без depth остаются совместимыми. Dual whitelist разрешает фактическое имя `summon_familiar`. Спека: `specs/dual-recursive-delegation.md`.
[2026-09-07] Добавлен persistent worker profile: Director-only `summon_familiar` принимает optional `role`, `specialization`, `workspace`, trim-ит значения и не сохраняет пустые поля. `inspect_agent` возвращает эти optional metadata read-only; legacy `familiars.json` без профиля читается. Role-based access и маршрутизация задач оставлены отдельными срезами. Спека: `specs/dual-worker-profile.md`.
[2026-09-07] `coordinate` получил optional `blocking: true`: default оставляет directive с important attention, а blocking coordination использует существующую очередь/checkpoints и bounded preemption без нового transport. Mailbox, permission graph и hard interrupt authority не изменены. Спека: `specs/dual-coordinate-escalation.md`.
[2026-09-07] Добавлена role-aware capability policy familiar: роль читается из parent `familiars.json` один раз при runtime startup. `tester` блокируется от `write/edit`, `designer` — от `write/edit/bash/job` и опасных job aliases; `developer`, `devops`, legacy и unknown roles сохраняют unrestricted behavior. Director boundary и one-level policy не менялись. Спека: `specs/dual-role-capabilities.md`.
[2026-09-07] `inspect_agent` расширен computed `currentTask` для running Task и `queue` только для queued/ready Tasks; полный `tasks` snapshot сохранён. Состояние вычисляется read-only из существующего Task store, без записи familiar state или нового transport. Спека: `specs/dual-inspect-worker-state.md`.
[2026-09-07] Проблема: live Dual PTY smoke с startup wait 1.5 секунды иногда не успевал загрузить Pi до `/dual on` → решение: использовать startup wait 4 секунды и отдельно ждать ответы команд; повторная проверка `LIVE_DUAL_ACTIVE_JOBS_SMOKE=PASS`. Правило и запись: `AI/mistakes/2026-09-07-live-dual-smoke-startup-wait.md`.
[2026-09-07] `inspect_agent` получил computed `activeJobs`, содержащий только `running` jobs; полный `jobs` snapshot, lifecycle и Task association сохранены. Состояние вычисляется read-only через существующий `listJobsForSession`. Спека: `specs/dual-inspect-active-jobs.md`.
[2026-09-07] `inspect_agent` получил computed `waitingTasks`, содержащий только `blocked` и `waiting_review`; queued/running и terminal Tasks остаются в полном `tasks`, но не попадают в waitingTasks. Scheduler/Task lifecycle не менялись. Спека: `specs/dual-inspect-waiting-tasks.md`.
[2026-09-07] `assign` получил optional `required_role`; scheduler сравнивает его с role familiar после normalization и возвращает typed `owner_role_mismatch` с recovery `assign_matching_role` в `inspect_queue`/`schedule`, не меняя mismatched Task. Legacy Tasks без requiredRole совместимы. Спека: `specs/dual-role-task-scheduling.md`.
[2026-09-07] Explicit `dispatch` теперь использует тот же role compatibility guard: matching `requiredRole` доставляется, mismatch возвращает `owner_role_mismatch` без mailbox/messages/Task mutation, а прежний manual override по dependency/scope/idle сохраняется. Спека: `specs/dual-role-explicit-dispatch.md`.
[2026-09-07] Добавлен role-aware review: `assign` принимает optional `required_reviewer_role`, а `review_task` проверяет role registered reviewer и возвращает structured `owner_role_mismatch` без inbox/Task mutation. Legacy review без поля сохраняется. При добавлении fixture была syntax error из-за закрывающего backtick; исправлено, правило записано в `AI/mistakes/2026-09-07-dual-role-review-test-template.md`. Спека: `specs/dual-role-review.md`.
[2026-09-07] Реализован per-familiar long-term memory: append-only `memory.jsonl` хранит `{ at, kind?, taskId?, summary }`. Familiar self-tool `commit_memory` (только внутри familiar sessions) и Director tool `record_worker_memory` (через зарегистрированный familiar) пишут trimmed/non-empty entries; `inspect_agent` возвращает `memory: { count, entries[<=20] }` в обратном хронологическом порядке. Legacy familiars без memory совместимы. Спека: `specs/dual-worker-memory.md`.
[2026-09-07] Реализован read-only `inspect_firm`: Director-only агрегатор возвращает `workers` (per-familiar summary), `tasks` (status breakdown + byOwner), `jobs` (status breakdown) и `attention` (`blocked`/`waiting_review` со ссылкой на blocker-сообщение). Familiar sessions получают `unauthorized_firm_inspection` без wake-up. Спека: `specs/dual-inspect-firm.md`.
[2026-09-07] Реализован `resolve_blocker`: Director tool закрывает blocker-цикл, переводя `blocked` Task в `ready` с audit `blocker_resolved` и уведомлением через `notifyTaskSubscribers`. Authorization: initiator или owner familiar через существующий `taskUpdateAuthorization`; другие сессии получают `unauthorized_resolve`. Не-resolvable states возвращают structured `task_not_blocked` без мутации. Тип TaskRecord расширен `blocker_resolved` для нового audit-record. Спека: `specs/dual-resolve-blocker.md`.
[2026-09-07] Реализован `amend_task`: Director tool прицельно обновляет спец-поля не-terminal Task (`goal`/`specification`/`workspace`/`requirements`/`constraints`/`acceptance_criteria`/`dependencies`/`scope`/`reviewers`) — только переданные непустые поля мутируются. Audit-record `amendment` всегда пишется при успешном вызове; подписчики получают уведомление через `notifyTaskSubscribers`. Terminal Tasks → `cannot_amend_terminal`; familiar sessions → `unauthorized_amend`. Не-отличия изменений → `nothing_to_amend`. `TaskRecord` расширен `amendment`. Спека: `specs/dual-amend-task.md`.
[2026-09-07] Реализован `reassign_task`: Director tool передаёт non-terminal Task между registered familiars, не меняя status/messages/revisions/testReports/jobs. Audit-record `reassignment` пишется с `from`/`to`/`sender`/`note`; подписчики уведомляются через `notifyTaskSubscribers`. Terminal Tasks → `cannot_reassign_terminal`; unknown `new_owner` → `unknown_familiar`; same owner → `same_owner`; familiar sessions → `unauthorized_reassign`. `TaskRecord` расширен `reassignment`. Спека: `specs/dual-reassign-task.md`.
[2026-09-07] Реализован `scope_conflicts`: Director-only read-only tool показывает пересечения `scope` между активными (non-terminal) Task разных owner-ов. Возвращает `groups` (`scope` + конфликтующие `tasks`/`owners`) и уникальный `taskIds`; `reason` = `scope_conflicts`/`no_conflicts`. Использует существующие `normalizedScope`/prefix-логику; terminal Task и пары с одним owner исключены. Ничего не пишет и не будит agents; familiar sessions → `unauthorized_scope_inspection`. Закрывает видимость молчаливого scheduler `scope_conflict`-блока. Спека: `specs/dual-scope-conflicts.md`.
[2026-09-07] Реализован `promote_request`: Director tool закрывает doc section 7 («`request` — новая работа, которую лучше превратить в task/queue item»). Читает request-сообщение из mailbox получателя (`readMailboxMessages` по `message_id`), создаёт queued Task через существующий `createTask` (`owner` = recipient или override; `goal`/`specification` фолбэчатся на текст сообщения), пишет audit-record `request_promoted` и проставляет `taskId` обратно в mailbox-строку (`linkMailboxMessage`) для трассировки и идемпотентности. Повтор → `already_promoted` без второй задачи; `type !== request` → `not_a_request`; неизвестное id → `unknown_message`; familiar sessions → `unauthorized_promotion`. Task не диспатчится автоматически. `TaskRecord` расширен `request_promoted`. Спека: `specs/dual-promote-request.md`.
[2026-09-07] Реализован `inspect_attention`: Director-only read-only инспектор входящего attention-потока одного сотрудника (doc section 8 — «отдельная очередь входящих событий с приоритетами», section 14 — структурный ответ без пробуждения LLM). Читает mailbox (`listMailboxMessages`/`readMailboxMessages`), оставляет события, чей recipient резолвится в целевой `sessionId` (`resolveFamiliarSession`, учитывает alias/id-ключи), и возвращает `inbound` — `total`, `byType`, `byPriority`, `topPriority` (max present из normal<important<blocking<interrupt) и `recent` (≤10, обратный хронологический порядок, `preview`≤120+…). Ничего не пишет и не будит agents; unknown `agent` → `unknown_agent`, familiar → `unauthorized_attention_inspection`. Не дополняет, а закрывает пробел: `inspect_agent` показывал задачи/jobs/память, но не поток сообщений. Спека: `specs/dual-inspect-attention.md`.
[2026-09-07] Реализован `flag_for_user` (doc section 19 «когда сообщить пользователю о значимом событии» + section 18 пользователь над Director-ом): Director-only tool дописывает машиночитаемый сигнал в durable-лог `dual/user-signals.jsonl` (append-only, как mailbox/память). `UserSignal = { id, at, kind, severity, summary, source, taskId? }`; `kind ∈ escalate|decision|blocker|completion|note` (неизвестный → `note`), `severity ∈ info|warning|critical` (невалидный → `info`). Пустой `summary` → `empty_summary`, familiar сессия → `unauthorized_flag`. Хвост (≤`USER_SIGNAL_INSPECT_LIMIT=20`, обратный хроно порядок) отдаётся новым полем `signals` в существующем `inspect_firm` — без отдельного read-tool. Это журнал для человека/UI, а не транспорт доставки: не будит агентов и не мутирует Task-и. Helpers: `userSignalsFile`/`readUserSignals`/`appendUserSignal`/`readUserSignalsTail`/`normalizeSignalKind`/`normalizeSignalSeverity`, `flagResult`, типы `FlagUserParams`/`FlagDetails`. Спека: `specs/dual-flag-for-user.md`.
[2026-09-07] Реализован `record_decision`/`inspect_decisions` (doc section 2/6/16/21: «Director фиксирует общие интерфейсы, инварианты и зависимости», «Developer может запросить уже принятый контракт», инвариант про shared interfaces). Durable append-only лог `dual/decisions.jsonl` (тот же паттерн, что mailbox/user-signals/worker-memory). `FirmDecision = { id: decision-<ms>-<uuid8>, at, kind, text, author, taskId? }`; `kind ∈ interface|invariant|dependency|decision` (напрямую из формулировок doc; неизвестный → `decision`). Director-only `record_decision(text, kind?, task_id?)`: пустой text → `empty_text`, familiar → `unauthorized_decision`, иначе `{decision,total}` reason `recorded`. Read-only `inspect_decisions(kind?, task_id?, limit?)` ДОСТУПЕН и familiar-сессиям (общий контекст фирмы, как `read`): фильтры, `limit` (default `DECISION_INSPECT_LIMIT=25`), обратный хронологический порядок, `{decisions,total}` reason `decisions_inspected`. Ни один не будит агентов; это журнал контрактов, а не транспорт доставки. Helpers: `decisionsFile`/`readDecisions`/`appendDecision`/`normalizeDecisionKind`, `decisionResult`/`decisionsInspectionResult`, типы `FirmDecision`/`FirmDecisionKind`/`RecordDecisionParams`/`InspectDecisionsParams`. Спека: `specs/dual-record-decision.md`.
[2026-09-07] Закрыта асимметрия `dispatch`/`schedule` (doc section 6/21: параллельность не «без явного решения Director»). `dispatch` раньше проверял только роль (`roleMismatchBlock`) и молча обходил scope-конфликт; теперь переиспользует `scopeConflict`/`schedulerBlock` и по умолчанию блокирует диспатч Task, чей `scope` пересекается с **running** Task другого владельца (structured `block.code = "scope_conflict"`, `relatedTaskId`, `recovery = "resolve_scope"`, task остаётся `queued`, envelope не отправлен). Новый опц. параметр `override_scope: true` — сознательное решение Director-а: диспатч проходит, в `task.messages` пишется audit-record `{ type: "scope_override", sender, at, relatedTaskId }`, и вызывается `appendUserSignal` (kind `escalate`, severity `warning`, `taskId`) — перекрёстная связь с section 19 «сообщить пользователю о значимом событии». Same-owner и непересекающиеся scope не затронуты (`scopeConflict` сам исключает same-owner); успешный override несёт `details.block` но без `error`. Spec-файл: `specs/dual-dispatch-scope-override.md`.
[2026-09-07] Авто-заполнен канал «фирма → пользователь» (doc section 19 «сообщить пользователю о значимом событии» + section 18 «история завершённых задач»). `dual_update_task` и авторизованный `cancel({ task_id })` — terminal entrypoints; helper `emitStatusTransitionSignal(task, previousStatus, source, reasonHint?)` после `writeTask` дописывает один `UserSignal` при переходе в `done`/`failed`/`cancelled` (kind `completion`; severity `warning` для `failed`, иначе `info`; summary `Task "<id>" (<goal>) → <status>` + ` — <decision/reason>`; `source` = сессия-обновившая; `taskId`). Идемпотентность: сравнение `previousStatus !== task.status` (повторный тот же терминал — без дубля), не-терминальные переходы сигнала не дают. Только append, authorization/transport не менялись; реюз `user-signals.jsonl` (без новых хранилищ); ручной `flag_for_user` остаётся для не-терминальных значимых событий. Regression на cancel path сначала воспроизвёл пропуск сигнала, затем исправлен. Спека: `specs/dual-task-outcome-signals.md`; mistake: `AI/mistakes/2026-09-07-dual-cancel-outcome-signal-missing.md`.
[2026-09-07] Расширен канал «фирма → пользователь» на blocked (doc section 19 «когда сотрудник считается blocked» + section 8 blocker = «не может продолжать без ответа»): `USER_SIGNAL_KINDS` уже имел значение `blocker`, но его никто не эмитил. Helper `emitTaskOutcomeSignal` обобщён до `emitStatusTransitionSignal(task, previousStatus, source, reasonHint?)` и вызывается из двух choke-points перехода в blocked: `dual_update_task` (status=blocked, reasonHint=message) и `message` type=blocker после `writeTask(blockerTask)` (reasonHint=текст сообщения). kind `blocker`, severity `warning`; идемпотентность та же (`previousStatus !== status`): повторный blocker на уже blocked — без дубля, `resolve_blocker` (→ready) сигнала не даёт, повторная блокировка после resolve = новый эпизод = новый сигнал. Только append, новых хранилищ/изменений потока управления нет. Спека: `specs/dual-task-outcome-signals.md`.
[2026-09-07] Ещё одно расширение той же ветки (doc section 18/19): закрыт второй «мёртвый» enum `decision`. `emitStatusTransitionSignal` дополнен переходом в `waiting_review` → `kind = "decision"`, `severity = "info"`, summary + ` — <message>` (работа готова к приёмке — «принять результат» section 18, «когда запустить review» section 19). Идемпотентность та же: повтор на уже `waiting_review` без дубля; переходы `running`/возврат в `ready` сигнала не дают. Реюз `user-signals.jsonl`, только append. Теперь в `USER_SIGNAL_KINDS` автомтически покрываются completion/blocker/decision (+escalate через dispatch-override), а `note` остаётся ручным дефолтом `flag_for_user`. Тест terminal-сигналов переименован и расширен до «terminal or review». Спека: `specs/dual-task-outcome-signals.md`.
[2026-09-07] (только тесты, рантайм не тронут) Добавлен acceptance-тест параллельной работы (doc section 22 п.2 «два Developer сокращают wall-clock»): два idle Developer + disjoint `scope` → один пульс `schedule` поднимает обе задачи в `running` одновременно, `skipped===[]`, разные владельцы, по одному envelope каждому; сравнение `scheduled` как МНОЖЕСТВО. ЗАодно починен давний flaky `scheduler respects required familiar roles…`: он звал `schedule limit:1` при двух валидных задачах и ас-сил `scheduled[0]===matchingTask.id`, но `readTasks()` tie-бреЙкит равный `createdAt` по random-UUID → недетерминированно. Переведено на `limit:2` + assert по отсортированному множеству + `skipped===[]` (смысл сохранён). ПРАВИЛО: тесты, где в один пуlse диспатчится несколько задач одной миллисекунды, сравнивают `scheduled`/`skipped` как множество, не по позиции. Dual-тесты: 28/28 ×5 прогонов зелёные (раньше flaky падал); полный набор 99/99. Спека: `specs/dual-mvp-parallel-proof.md`.
[2026-09-07] Исправлен реальный баг привилегий (doc section 12 / invariant 21: «hard interrupt — только пользователю, Director и критическим системным событиям; сотрудники не должны бесконечно перебивать друг друга»). Tool `interrupt` гейтился корректно (`if (parentSessionId()) → cannot send hard interrupt`), но общий `message` принимал `type:"interrupt"` и `attention:"interrupt"` БЕЗ проверки отправителя: familiar мог прислать envelope с `priority:"interrupt"`, приёмник в `shouldAbortIncomingMessage` вызывал `abortCurrentTurn()` — обход hard-interrupt гейта. Permission-graph-тест проверял только `interrupt`-tool, поэтому дыра не ловилась. Фикс: в начале `message.execute` если (`type==="interrupt"` || `attention==="interrupt"`) && `parentSessionId() !== null` → структурная ошибка `Error: familiar sessions cannot send hard interrupt; use blocker or coordinate`, `error:true`, ДО любых writeMessage/envelope (нулевые сайд-эффекты). Director (`parentSessionId()===null`) по-прежнему может; blocker/directive/coordinate — легальный путь эскалации. Регресс-тест в permission-graph: familiar message(type=interrupt) и (type=info,attention=interrupt) отклонены, Director message(interrupt) доставлен. Полный набор 99/99 зелёный, tsc OK, Pi 0.75.5, git diff --check, LIVE_DUAL_INTERRUPT_PRIV_SMOKE=PASS. Спека: `specs/dual-message-interrupt-privilege.md`.
[2026-09-07] Продолжение аудита инвариантов: закрыт обход control-plane привилегии. Tool-гейт dual (`isDual`+whitelist) действует только на Director-сессии, поэтому каждый dual-инструмент обязан иметь ВНУТРЕННЮЮ проверку. `assign`/`dispatch`/`schedule` её НЕ имели (в отличие от `interrupt`/`flag_for_user`/`record_decision`/`promote_request`): familiar-сессия могла сама создавать Tasks, запускать чужие и гонять scheduler — нарушение «Director распределяет работу» (section 5/19/21) и обход Director-only promote_request. Существующие тесты это не ловили (все вызовы шли из Director-контекста). Фикс: ранний `if (parentSessionId() !== null) → структурированный отказ` в начале каждого из трёх execute (до любых записей; assign familiar-а не создаёт файл задачи). Worker-эскалация новой работы остаётся через message(request)→Director promote_request или coordinate. `schedule` и ранее был initiator-scoped — gate как defense-in-depth за единый инвариант. Новый тест «assign, dispatch, and schedule are Director control-plane actions» + Director-assign позитивный кейс. Dual 29/29×3, полный набор 100/100 зелёный, tsc OK, Pi 0.75.5, git diff --check, LIVE_DUAL_CONTROL_PLANE_SMOKE=PASS. Спека: `specs/dual-control-plane-director-only.md`.
[2026-09-07] Аудит инвариантов продолжен, третий случай того же класса: у `record_worker_memory` («on behalf of a familiar FROM Director») не было явного Director-гейта — защищался лишь имплицитно `findFamiliar(sender)` (пустой реестр familiar → unknown_familiar). Хрупко: рефакторинг резолвинга мог открыть familiar-у запись чужой памяти. Добавлен явный `parentSessionId() !== null → отказ` (reason «familiar sessions cannot record worker memory»); self-запись = familiar-tool `commit_memory`. Тест memory-расширен familiar-denial (память не пишется). ЧЕСТНЫЙ АУДИТ остального: `cancel` (job: sessionID/linked-task без reviewer; task: authz&&!reviewer), `dual_update_task` (reviewer ограничен waiting_review/done/failed; owner self-done — намеренный MVP-поведение, НЕ меняю), `subscribe` (ask-target безвреден: subscriber===requester при доставке) — обходов нет. Dual 29/29×3, полный набор 100/100, tsc/Pi 0.75.5/diff OK, LIVE_DUAL_MEM_GATE_SMOKE=PASS. Спека: `specs/dual-control-plane-director-only.md`.
[2026-09-07] Добавлен сквозной acceptance-тест MVP (doc section 22 «доказать 4 вещи»; только тесты, рантайм не тронут): Director блокирован от write/edit/bash (tool_call-хук) но декомпозит (assign×2 disjoint scope) и раздаёт → обе running параллельно (п.1, п.2); harness доставляет busy-воркеру directive (envelope priority important) и blocker (Task→blocked, проверен inspect_task) без добровольного polling моделью (п.4); п.3 доказан в familiar-сините (buildAskArguments consult=read-only). Хелпер теста readEnvelope ищет конверт по regex без учёта регистра (envelope помечен `[Dual <type> …]` — заглавная D). Найденный при отладке баг самого теста: регистрозависимый regex + ожидание priority important у plain directive (plain `message(type:directive)` без attention даёт normal; importance даёт coordinate) — починено явным attention:important. Dual 30/30×3, полный набор 101/101, tsc/Pi 0.75.5/diff OK, LIVE_DUAL_SECTION22_SMOKE=PASS. Спека: `specs/dual-mvp-section22-e2e.md`.
[2026-09-07] Проблема: `dispatch` был Director-only, но при общем `AI_DATA_HOME/dual/tasks` не проверял `task.initiator === sender`; другой root-Director мог dispatch-ить чужую Task в совпадающий owner ID своего familiar → Решение: ранний `unauthorized_dispatch` после чтения Task и sender, до `resolveFamiliarSession`/mailbox/writeTask/user-signal; собственный Director path сохранён. Regression покрывает чужой root, пустой inbox/signals и позитивный dispatch владельца. Правило: Director-only недостаточно для shared storage — control-plane mutation обязан проверять принадлежность ресурса текущему initiator. Dual 31/31×3, полный Node 102/102, tsc, gofmt/vet/test/build, Pi 0.75.5, diff OK, LIVE_DUAL_DISPATCH_AUTH_SMOKE=PASS. Спека: `specs/dual-dispatch-initiator-authorization.md`; mistake: `AI/mistakes/2026-09-07-dual-dispatch-initiator-bypass.md`.
[2026-09-07] Проблема: `taskUpdateAuthorization` разрешал owner через прямое `sender === task.owner` до проверки `familiars.json`; root Director B мог совпасть с unknown owner Task Director A и менять/отменять/подписываться на чужую Task → Решение: owner разрешается только через registry Task initiator по familiar `id` или зарегистрированному `sessionId`; initiator Director и reviewer path сохранены. Regression проверяет отсутствие update/subscription для foreign root и позитивные owner ID/sessionId paths. Dual 32/32×3, полный Node 103/103, tsc, gofmt/vet/test/build, Pi 0.75.5, diff OK, LIVE_DUAL_OWNER_AUTH_SMOKE=PASS. Спека: `specs/dual-task-owner-registry-authorization.md`; mistake: `AI/mistakes/2026-09-07-dual-owner-direct-match-bypass.md`.
[2026-09-07] Проблема: `promote_request` был только Director-only, но принимал произвольный mailbox `recipient/message_id`; Director B мог промотировать request Director A, создать чужую Task и переписать mailbox `taskId` → Решение: provenance guard до type/taskId и side effects: разрешён собственный `message.sender === sender` или зарегистрированный familiar sender из `readFamiliars(sender)`. Recipient может оставаться unknown, чтобы сохранить queued request flow. Regression проверяет foreign отказ без Task/mailbox mutation, self request для unknown recipient и familiar request. Dual 33/33×3, полный Node 104/104, tsc, gofmt/vet/test/build, Pi 0.75.5, diff OK, LIVE_DUAL_PROMOTE_AUTH_SMOKE=PASS. Спека: `specs/dual-promote-request-firm-authorization.md`; mistake: `AI/mistakes/2026-09-07-dual-promote-cross-root-bypass.md`.
[2026-09-07] Проблема: generic `inspect` не имел authorization и раскрывал familiar все Tasks, включая чужие goal/specification/workspace; при `task_id` дополнительно выполнялся jobs lookup → Решение: ранний `parentSessionId() !== null` → `unauthorized_inspection` с пустым результатом до чтения Task/jobs. Director filters сохранены, `inspect_task`/`inspect_decisions` не изменены. Regression проверяет familiar denial и scoped positive path. Dual 34/34×3, полный Node 105/105, tsc, gofmt/vet/test/build, Pi 0.75.5, diff OK, LIVE_DUAL_INSPECT_BOUNDARY_SMOKE=PASS. Спека: `specs/dual-generic-inspect-boundary.md`; mistake: `AI/mistakes/2026-09-07-dual-generic-inspect-disclosure.md`.
[2026-09-07] Проблема: `inspect_firm` фильтровал workers/jobs по текущему root, но Task summary/attention брал из общего storage; `scope_conflicts` сравнивал active Tasks всех root-ов → Решение: оба агрегатора фильтруют Tasks по `task.initiator === sender`; Director B больше не видит Task/scope conflict Director A, а два owner-а внутри одной фирмы по-прежнему видны. Regression и прежние scope/firm tests проходят. Dual 35/35×3, полный Node 106/106, tsc, gofmt/vet/test/build, Pi 0.75.5, diff OK, LIVE_DUAL_FIRM_PROVENANCE_SMOKE=PASS. Спека: `specs/dual-firm-inspection-provenance.md`; mistake: `AI/mistakes/2026-09-07-dual-firm-inspection-cross-root.md`.
[2026-09-07] Проблема: `resolve_blocker` принимал общий `taskUpdateAuthorization.authorized` и reviewer мог снять blocker, хотя контракт разрешает только initiator/owner → Решение: операция отклоняет `authorization.reviewer` с `unauthorized_resolve`; owner/initiator flow и reviewer `dual_update_task` сохранены. Regression в resolve lifecycle; Dual 35/35×3, полный Node 106/106, tsc, gofmt/vet/test/build, Pi 0.75.5, diff OK, LIVE_DUAL_REVIEWER_BLOCKER_SMOKE=PASS. Спека: `specs/dual-resolve-blocker.md`; mistake: `AI/mistakes/2026-09-07-dual-reviewer-resolve-bypass.md`.
[2026-09-07] Проблема: familiar `ask`/`tell_familiar` искали target в registry child session, который пуст при one-level delegation; быстрые tell использовали `${Date.now()}.json` и могли перезаписывать сообщение → Решение: единый `communicationRegistrySession` использует `PI_OWNER_SESSION` для sibling lookup, tell принимает ID/full session зарегистрированного familiar, inbox filenames используют `${Date.now()}-${randomUUID()}.json` (также `tell_owner`). Regression parent registry/unknown target/two rapid messages. Full Dual 35/35×3, Node 107/107, tsc, gofmt/vet/test/build, Pi 0.75.5, PTY runtime marker `LEN=6886 VERSION=True ERROR=False`, diff OK. Спека: `specs/familiar-parent-registry-communication.md`; mistake: `AI/mistakes/2026-09-07-familiar-parent-registry-communication.md`.
[2026-09-07] Проблема: `ask` и `ask_owner` строили numeric `ask_id` только через `Date.now()`, поэтому same-millisecond calls colliding за map/files → Решение: monotonic `allocateAskId`, сохраняющий numeric timestamp semantics и выдающий следующий integer при collision. Regression одинакового timestamp; full Node 108/108, Dual 35/35×3, tsc, gofmt/vet/test/build, Pi 0.75.5, diff-check; PTY startup diagnostic previously verified runtime marker. Спека: `specs/familiar-ask-id-uniqueness.md`; mistake: `AI/mistakes/2026-09-07-familiar-ask-id-collision.md`.
[2026-09-07] Проблема: `subscribe_familiar` и `unsubscribe_familiar` принимали arbitrary `who`/`to`; familiar мог подписать/отписать чужую сессию, а idle callback записывал ей inbox, unknown IDs тоже создавали durable subscription/path → Решение: оба tools валидируют registry: root работает с registered entries, familiar — только от себя через parent registry; unknown → `unknown_familiar_subscription`, forged who → `unauthorized_subscription`, invalid/no-op paths не вызывают `saveSubs`, idle filenames timestamp+UUID. Regression valid/duplicate/self-sibling/forged/unknown/rapid idle и forged/unknown unsubscribe. Full Node 109/109, Dual 35/35×3, tsc, gofmt/vet/test/build, diff-check; PTY diagnostic marker ранее verified. Спека: `specs/familiar-subscription-authorization.md`; mistake: `AI/mistakes/2026-09-07-familiar-subscription-authorization.md`.
[2026-09-07] Проблема: Dual durable stores (`tasks`, mailbox, subscriptions, user-signals, decisions, settings) жили в глобальном `AI_DATA_HOME/dual`/session path вне Automata Director folder; familiar ask completion также читал старый global subscriptions path → Решение: Dual использует `AUTOMATA_HOME/profiles/<profile>/sessions/<firmSessionId>/dual`, где root scope = текущий Director, familiar scope = `PI_OWNER_SESSION`; все readers/writers и ask completion parent lookup переведены на этот scope, старый global path не создаётся/не читается. Regression 36 Dual + 15 familiar; shared identity roots изолированы. Full Node 110/110, tsc, gofmt/vet/test/E2E/build, diff-check PASS. Спеки: `specs/dual-session-scoped-storage.md`, `specs/dual-inspect-agent-provenance.md`; mistake: `AI/mistakes/2026-09-07-dual-global-storage-boundary.md`.
[2026-09-07] Проблема: Automata `internal/paths.BaseDir` использует `AI_DATA_HOME`, а Dual/familiar/job использовали разные base-resolvers (`AUTOMATA_HOME` мог увести state и lifecycle messages в другой root) → Решение: единый порядок `AI_DATA_HOME` → `AUTOMATA_HOME` → `~/.ai/automata` в `dual/index.ts`, `familiar/index.ts` и `job/index.ts`; regressions AI-only, AI-over-AUTOMATA и fallback зелёные. Финал: Dual 37/37, familiar 15/15, job 9/9, Node 112/112, tsc, Go vet/test/E2E/build, scoped diff-check; live PTY 2/2 `LIVE_DUAL_CANONICAL_HOME_SMOKE=PASS`. Спека: `specs/dual-automata-home-alignment.md`; mistake: `AI/mistakes/2026-09-07-dual-automata-home-env-mismatch.md`.
[2026-09-08] Аудит familiar-tab выявил: close-control использовал ASCII `x` и имел лишнюю третью ячейку; outside-click оставлял modal открытым; удаление tab перед активным сдвигало `activeIdx`; `PtyExitMsg` Main мог удалять Main через familiar cleanup и затем не доходил до Main panel → Решение: Unicode `×` в двухклеточной зоне (`len(name)+4`), outside-click dismiss без forwarding, общий `removeSessionAt` с коррекцией activeIdx, familiar-only `removeDeadFamiliar` с возвратом результата и routing Main PtyExit в собственный panel. Добавлены regressions `TestRenderTabBarColorsFamiliarGrey`, `TestClickOutsideCloseModalCancels`, `TestRemoveFamiliarBeforeActiveKeepsActiveTab`, `TestDeadFamiliarPtyExitRemovesMatchingTab`, `TestMainPtyExitStaysInMainTabAndRoutesToPanel`, `TestConfirmEscLeavesTabIntact`. Спека: `specs/familiar-tab-color-and-close.md`; mistakes: `AI/mistakes/2026-09-08-familiar-tab-regression-fixture.md`, `AI/mistakes/2026-09-08-cuetty-automata-chat-select.md`, `AI/mistakes/2026-09-08-cuetty-lines-coordinate-prefix.md`, `AI/mistakes/2026-09-08-cuetty-stale-binary-after-code-change.md`.
[2026-09-08] Проверки familiar-tab: `go vet ./...`, `go test ./... -count=1 -p 1`, `go build -o automata .` зелёные; свежий cuetty smoke с двумя familiar tabs, modal Y и сохранением Main — 2/2 `CUETTY_DUAL_TWO_FAMILIAR_CLOSE_SMOKE=PASS`; storage smoke с реальными JSONL и `familiars.json` — 2/2 `CUETTY_DUAL_TWO_FAMILIAR_STORAGE_SMOKE=PASS`. Перед live smoke бинарь обязательно пересобирается; mistake: `AI/mistakes/2026-09-08-cuetty-stale-binary-after-code-change.md`.
[2026-09-07] Проблема: broad git diff/stat в репозитории с личными runtime records вывел посторонние session/job paths → Решение: использовать scoped `git diff -- <точные Dual-файлы>` и `git diff --check`; mistake: `AI/mistakes/2026-09-07-broad-git-diff-noise.md`.
[2026-09-07] Проблема: broad grep по `/Users/a` заходил в системные каталоги и выдавал permission noise → Решение: искать только в целевом repo/разрешённых glob-путях. Mistake: `AI/mistakes/2026-09-07-broad-home-grep-permission-noise.md`.
[2026-09-07] Проблема: `TestContextPanelNotesWatcherClosesOnDomainChange` синхронно ожидал закрытие fsnotify Events после `Watcher.Close()` и флакал → Решение: тест ждёт asynchronous channel close bounded timeout 1s; production watcher code не изменён. Targeted ×20 PASS; mistake: `AI/mistakes/2026-09-07-automata-fsnotify-close-async-test.md`.
[2026-09-07] Problem: workspace runtime fixture с содержимым `.idle` равным `idle` определялся как busy → Solution: тестовые idle markers записываются числовым timestamp, как ожидает `readAgentRuntime`.
[2026-09-07] Problem: preview fixture с alias familiars зависел от случайного tie-break ID → Solution: assertions проверяют количество ready/blocked и set block codes, а не порядок случайно созданных Task.

[2026-08-25] Проблема: × на familiar-табе удалял в UI, но фамильяр восстанавливался при следующем poll. Две причины: (1) `FamiliarsJSONLPath` строил путь без slug профиля (`HumanHorizon` vs `humanhorizon` на диске) — `os.IsNotExist` → `RemoveFamiliar` тихо no-op. (2) `closeFamiliarByID` возвращал `tea.Cmd`, но Modal.Action (callback `func()`) не мог его пробросить — клик на кнопку Yes терял cleanup. → Решение: `FamiliarsJSONLPath` использует `ProfileSlug`; `closeFamiliarByID` стал синхронным (без cmd), `closeFamiliarCmd` в main.go переименована в `closeFamiliar` и вызывается напрямую. Оба пути (Y-клавиша и клик на Yes) теперь идентичны.

[2026-07-15] **Familiar tab color + close button** — добавлен серый цвет неактивной вкладки familiar-сессии (`#3c3836`) и синий активной (`#1f4e79`); красная кнопка `×` справа от каждого familiar-таба. Клик на `×` открывает Warp Modal (`warp.Modal.Overlay`) с подтверждением "Close `<name>`?" и кнопками `[Yes]`/`[No]`. Y/N/Esc тоже работают. Да — останавливает эмулятор, удаляет JSONL и убирает запись из `familiars.json`. Реализация: `paths.RemoveFamiliar`, `(*App).closeFamiliarCmd`, поля `pendingCloseFamiliar` + `closeFamiliarModal` в `ChatPanel`, обработчики в `handleMouse`/`Update`. 4 новых теста + 3 для `RemoveFamiliar`. Все unit/e2e зелёные, включая `TestChatOpensWithPlanPane` (раньше флакал).

[2026-07-10] Проблема: PTY-сообщения неактивных Pi-чатов терялись в текущем дереве Warp, цепочка `Emulator.Listen()` прекращалась и заполненная очередь блокировала `just-pi` → Решение: `App.Update` напрямую маршрутизирует `PtyReadyMsg`, `PtyOutputMsg` и `PtyExitMsg` в cached emulator по `SessionID`, а неизвестные сессии оставляет обычной маршрутизации Warp.

[2026-07-10] Проблема: `git diff` был запущен в каталоге Automata без Git-репозитория → Решение: перед Git-командами проверять наличие репозитория; в Automata проверять целевые файлы напрямую и не считать недоступный `git diff --check` успешной проверкой.

[2026-07-10] Проблема: после исправления был выполнен `go build ./...`, который проверил компиляцию, но не обновил используемый файл `./automata`; пользователь продолжил работать на старом бинарнике → Решение: после изменения Automata обязательно выполнять `go build -o automata .`, проверять mtime executable и тестировать свежий процесс; уже запущенные экземпляры нужно перезапустить без принудительного завершения пользовательских сессий.

[2026-07-10] Проблема: первый сценарий cue-tty запустил `./automata` через daemon и завершился `write /dev/ptmx: input/output error` из-за ненадёжного относительного пути → Решение: для запуска через cue-tty daemon использовать абсолютный путь к executable и не засчитывать сценарий без итоговых артефактов и маркеров.

### 2026-06-30 — E2E-тест панели Chat+Plan проходит
- **Корневая причина**: `Container.View` вызывал `innerTab.Update(ResizeMsg)`, что запускало `ContextPanel` для старта ai-knowledge, но `View` не возвращает команды — команды `StartWithEnv`/`Listen` терялись.
- **Исправление**:
  1. Добавлен `StartSync` в Emulator — запускает PTY синхронно (без tea.Cmd).
  2. `Container.View` больше не вызывает `innerTab.Update` — только рендерит.
  3. `Container.Update` обрабатывает `ResizeMsg` и устанавливает долю разделения.
  4. `TabGroup.Update` теперь обрабатывает `ResizeMsg` (раньше проваливался в `broadcastMsg` с сырым размером warp).
  5. `Tab.Update` передаёт неизвестные сообщения (`PtyOutputMsg` и т.д.) через `broadcastMsg` дочерним элементам.
  6. `ContextPanel` запускает ai-knowledge синхронно в `SetPlanSession`.
  7. Флаг `needsListen` гарантирует однократный вызов `Listen()` после синхронного запуска.

### 2026-06-29 — Контейнер переработан на примитивы Warp
- `ContextPanel.Update` теперь транслирует `warp.ResizeMsg` в `portalis.ResizeMsg`, чтобы эмулятор ai-knowledge получал корректный размер панели до запуска PTY.
- `warp.Tab.broadcastResize` собирает команды, возвращаемые листовыми панелями; `BroadcastResize` возвращает `tea.BatchMsg`, чтобы bubbletea выполнял команды запуска боковых панелей (например, панель планов ai-knowledge).
- `Container.updateChatWithPlanMode` транслирует `ResizeMsg` контейнера в размеры, специфичные для разделения чат-терминала и панели планов.

## Известные проблемы
- Чат-сессии `just-pi` теперь по умолчанию используют `PI_SKIP_VERSION_CHECK=1`, чтобы модалка «Update Available» не блокировала TUI чата.

## Соглашения
- Сначала спецификация для новых фич (спецификации в `specs/`).
- Минимум логирования; файлы отладочных логов — no-op в production-пути.
- Использовать `go vet`, `go build`, `go test` после каждого изменения.
- Никаких коммитов от AI.

[2026-07-11] Проблема: PTY-батчинг повторно запускал Listen() из RenderTickMsg и не маршрутизировал RenderTickMsg по SessionID, из-за чего ANSI-последовательности разрывались между параллельными читателями, а фоновые чаты не обновлялись → Решение: оставить единственную listener-цепочку в PtyOutputMsg, не запускать Listen() из RenderTickMsg и маршрутизировать RenderTickMsg в кэшированный эмулятор по SessionID.
[2026-07-11] Проблема: ChatPanel.renderTabBar измерял ANSI-строку через len и обрезал bar[:width], разрывая CSI/SGR-последовательности и повреждая нижнюю часть терминала → Решение: использовать ansi.StringWidth и ansi.Truncate, проверять видимую ширину regression-тестом.
[2026-07-11] Проблема: команда gofmt была запущена из корня с путями без internal/ui → Решение: перед запуском учитывать workdir и использовать полные относительные пути от него.
[2026-07-11] Проблема: regression-тест создал []chatSession вместо фактического []*chatSession → Решение: перед созданием fixture проверять точный тип поля структуры.

[2026-07-11] Проблема: Portalis считал wide Unicode/emoji одной ячейкой, из-за чего экран чата становился шире панели и status bar повреждался → Решение: внедрена wide/combining cell-модель Portalis с continuation-ячеями; Automata пересобрана.
[2026-07-12] Проблема: отключение mode 2026 по replay без chunk/resize timeline не убрало живое размазывание, а reset/skip cursor были недопустимыми эвристиками → Решение: восстановить стандартный synchronized output, убрать RenderTick с задержкой 16 мс, сохранять ordered PTY reads по 4 КБ и лениво коммитить synchronized frame только в следующем View; cue-tty stress 400 кадров проходит за 35 мс без размазывания.
[2026-07-13] Проблема: Clear искал JSONL только в каталоге текущего cwd, поэтому не очищал AI#2, созданную в /Users/a/Space и позднее открытую в /Users/a/Space/Projects/HumanHorizon/automata → Решение: сначала искать точное совпадение id в текущем cwd, затем во всех каталогах хранилища и при нескольких совпадениях выбирать самый новый JSONL.
[2026-07-13] Проблема: `git status` повторно запущен в Automata, хотя в контексте уже записано отсутствие Git-репозитория → Решение: перед Git-командами сначала читать CONTEXT.md и проверять наличие `.git`; при его отсутствии не запускать Git вовсе.
[2026-07-13] Проблема: ширина кнопки `× Clear` вычислялась через `len`, поэтому двухбайтовый UTF-8 символ `×` считался двумя колонками и tab bar был на одну колонку короче → Решение: вычислять ширину кнопки через `ansi.StringWidth`; целевой UI-тест проходит.
[2026-07-13] Проблема: диагностический `rm -f /tmp/e2e-terminal.raw*` завершился в zsh ошибкой `no matches found`, а попытка заменить его на `find -delete` была заблокирована политикой безопасности → Решение: не удалять диагностические файлы и сразу использовать уникальный trace base с timestamp.
[2026-07-13] Проблема: новые terminal и chat-сессии в Automata не показывали prompt и не принимали клавиатуру, хотя PTY запускался и raw trace содержал prompt → Решение: устранён Portalis deadlock — Emulator.Update не повторно захватывает e.mu в CWD callback и передаёт изменения OnCWDChange после Unlock; причина локализована в Portalis, в Automata только зависимость от его исправления. Полный E2E Automata проходит.
[2026-07-15] Проблема: CPU Automata линейно рос при простое, потому что `KnowledgeRefreshMsg` каждые пять секунд и `WindowSizeMsg` создавали дополнительные вечные цепочки `blinkCmd` → Решение: оставить продолжение blink-цепочки только в обработчике `blinkMsg`; regression-тесты подтверждают отсутствие таймеров у refresh/resize, отдельный свежий экземпляр держит в среднем 0.25% CPU при простое.
[2026-07-15] Проблема: после `stream → Escape → новый stream` в чате появлялись 2–3 строки Working и дубли разделителей; причина — несколько concurrent Portalis `Listen` для одного PTY, которые нарушали порядок chunks → Решение: в Portalis введён single-flight listener guard, `AlreadyRunning` не запускает новый reader; из TermPanel удалено неограниченное full-frame debug-логирование. Portalis vet/test и Automata vet/test/build проходят; live post-fix abort-сценарий наблюдал максимум 1 Working. Старый 40-ГБ `/tmp/automata-termpanel-debug.log` удалён по разрешению Ани.
[2026-07-15] Проблема: после Clear новый pi запускался без `PI_CODING_AGENT_DIR`, а возвращённая из команды вложенная `tea.Cmd` не исполнялась, поэтому чат завершался без перезапуска → Решение: запускать новый PTY через `StartSync` с подготовленным окружением и возвращать `PtyReadyMsg`; добавлен регрессионный тест каталога агента и автоматического перезапуска.
[2026-07-15] Проблема: первый полный `go test ./... -count=1` получил таймаут модалки в `TestChatOpensWithPlanPane`, хотя остальные пакеты прошли → Решение: выполнить адресный повтор теста; повторный запуск прошёл, считать исходный сбой флапом E2E и не менять production-код без воспроизведения.

[2026-07-16] Проблема: scrollback terminal-сессий сохранял строки с исходной шириной `s.Cols`; после resize (особенно уменьшения ширины) старые строки scrollback оставались длиннее текущего `s.Cols`, и правая часть визуально обрезалась, а в `renderCells` цикл `for c := 0; c < s.Cols; c++` уже сам обрезал хвост → Решение: в `Screen.resize()` после обновления `s.Cols` вызвана `normalizeScrollback()`, которая подгоняет каждую строку `s.scrollback` до новой ширины (truncate с дропом dangling continuation, pad blank cells), плюс clamp `viewOffset` на случай если scrollback уменьшился; добавлены три регрессионных теста (`TestResizeShrinkTruncatesScrollback`, `TestResizeGrowPadsScrollback`, `TestResizeScrollbackDropsDanglingContinuation`), portalis vet/test проходит, automata vet/test/build проходит, пересобран `./automata`.

[2026-07-17] Проблема: `× Clear` очищал только главную сессию, оставляя фамильяров этой сессии жить с их JSONL и табами → Решение: в `clearSessionCmd(sessionID, cwd, familiarSIDs)` добавлена фаза 0, которая останавливает эмуляторы фамильяров, удаляет их JSONL и очищает `familiars.json` (`paths.ClearFamiliarsJSONL` пишет `[]`, чтобы `checkFamiliars` убрал табы); в `ChatPanel` добавлены экспортёры `Sessions()`, `Em()`, `FamiliarID()` и метод `FamiliarSessionIDs()`; `familiarStatePath()` теперь делегирует в `paths.FamiliarsJSONLPath`; callback `SetOnClearSession` собирает `cp.FamiliarSessionIDs()` и передаёт в `clearSessionCmd`; добавлены регрессионные тесты `TestClearKillsFamiliarsOfThisSession` и `TestClearKillsFamiliarsRespectsProfile` в `main_view_test.go`, плюс `TestFamiliarSessionIDsReturnsNonMainOnly`, `TestFamiliarSessionIDsEmptyForMainOnly`, `TestSessionsExporter` в `chat_panel_test.go`; порядок важен — стоп эмуляторов до удаления JSONL до очистки `familiars.json`, иначе живой фамильяр перепишет файл; баг `clear-session-replace-panel-em.md` (пустой экран главного таба после Clear) остаётся отдельной задачей.
[2026-07-18] Проблема: чаты из дерева запускались как «обычный pi» без `PI_CODING_AGENT_DIR`, сессии писались в дефолтный `~/.pi/agent/sessions` (доказательство: `keller__wildberries.ai.jsonl` от 18.07). Корень: три несогласованных резолвера pi-команды; `SetOnSelectChat` игнорировал `a.piAgentDir`, полагался на `just-pi` из PATH, не выставлял `AUTOMATA_PROFILE` и молча деградировал в шелл; старт через `ItemSelectedMsg → em.Start() = StartWithEnv(nil)` терял env всегда → Решение: единый `App.piLaunch(sessionID)` (PI_CMD override → /usr/local/bin/pi + PI_CODING_AGENT_DIR + AUTOMATA_PROFILE → just-pi fallback → nil без деградации в шелл); в Portalis добавлены `startEnv`/`SetStartEnv`/`StartEnv`, `Start`/`StartWithEnv(nil)`/`StartSync(nil)` используют сохранённый env, поэтому любой путь старта теперь сохраняет окружение; `createChatEmulator`/`createFamiliarEmulator`/`SetOnSelectChat`/`restoreSessions`/`SetOnTaskAssigned`/`clearSessionCmd` переведены на него; поле `pendingEmulatorEnv` удалено; `createChatEmulator` сам обрабатывает `IsTerminal` (шелл без pi-env); тесты `TestPiLaunchSetsAgentDirAndProfile`, `TestPiLaunchHonorsPiCmdOverride`, `TestCreateChatEmulatorWithoutPiCommandReturnsNil`, `TestCreateChatEmulatorTerminalUsesShellWithoutPiEnv`, обновлён `TestClearRestartsChatWithConfiguredPiAgentDir`; vet/test/e2e зелёные, бинарник пересобран. Сиротские сессии в `~/.pi/agent/sessions/` (5 шт.) НЕ перенесены — ждёт решения Ани. Спека: `specs/unify-pi-launch-env.md`.
[2026-07-23] Проблема: выделение текста в чате после прокрутки смещалось относительно видимых строк из-за несогласованных viewport- и scrollback-координат → Решение: в локальном Portalis выделение переводится в логические строки (`row - viewOffset`), а рендеринг и SelectionText используют общую систему координат; добавлены регрессионные тесты. Automata `go vet`, `go test` и `go build -o automata .` проходят.
[2026-07-23] Проблема: точечное редактирование CONTEXT.md не сработало из-за несоответствия точного текста строки → Решение: перед повторным редактированием читать актуальный фрагмент файла и копировать его без реконструкции.
[2026-07-23] Проблема: полный `go test ./...` в Automata получил единичный таймаут `TestChatOpensWithPlanPane` при открытии модалки → Решение: адресный повтор `go test ./e2e -run TestChatOpensWithPlanPane -count=1 -v` прошёл; считать это известным E2E-флапом, production-код не менять.
[2026-07-31] Проблема: агент не мог восстановить ID собственных активных jobs, недельная очистка могла удалить метаданные живого процесса, а правая панель кешировала только mtime каталога `jobs/` и могла исключить живой PID при временной ошибке `ps` → Решение: добавлен `job_list` текущего чата; running jobs никогда не удаляются, завершённые ограничены 40 последними на чат и неделей; кеш учитывает все вложенные `job.json`; существование PID сначала проверяется через `kill(0)`, а ошибка `ps` не скрывает живую job; добавлены TypeScript и Go regression-тесты, полный vet/test/build зелёный. Спека: `specs/reliable-active-jobs.md`.
[2026-07-31] Проблема: изолированный TypeScript 6 typecheck с явным файлом дважды не стартовал из-за отсутствия `--ignoreConfig`, затем Node-типов → Решение: для file-specific проверки extension использовать `pnpm exec tsc --ignoreConfig --types node ...`; тесты с production-импортом `.js` предварительно компилировать в `node_modules/.cache`.
[2026-07-31] Проблема: точечная замена типа `ctx` захватила соседние строки и временно повредила синтаксис callback → Решение: при замене блока сохранять в `newText` весь совпавший регион и сразу перечитывать изменённый фрагмент до запуска проверок.
[2026-07-31] Проблема: правая панель Automata зависела от 5-секундного `RefreshKnowledgeCmd` в `main.go` и от fallback `kanbanTickMsg`; живые jobs часто пропадали или появлялись с задержкой, а `kanbanFallbackInterval` дублировал работу fsnotify → Решение: вся правая панель теперь event-driven — `KnowledgePanel.setupWatchers()` создаёт два `fsnotify.Watcher` (на каталог сессии для status/plans/settings и на jobs/), обрабатывает `knowledgeChangedMsg`/`jobsChangedMsg` в `Update`, re-arm через blocking Cmd; `Refresh()` оставлен для совместимости; в `main.go` удалены `lastKnowledgeRefresh` и `RefreshKnowledgeCmd` из blinkMsg; `KanbanPanel` потерял `kanbanTickMsg`/`tickCmd`/`tickPending`/`kanbanFallbackInterval` (Аня подтвердила, что fallback не нужен); `TestNoKanbanTickFallbackExists` гарантирует отсутствие возврата. Бинарник пересобран 19:47. Спека: `specs/watch-jobs-panel.md`.
[2026-08-01] Проблема: после перевода панели на fsnotify живая job могла исчезнуть из списка, если `ps -o lstart=` отказывал или 2-секундное окно не покрывало clock skew macOS kqueue; `List` ещё и сам перезаписывал `job.json` со статусом `exited`, скрывая причину пропадания → Решение: `pidIsSameProcess` теперь опирается на `syscall.Kill(pid, 0)` как единственный источник истины, `ps` используется как подтверждение с расширенным окном 5 секунд; добавлен `pidIsSameProcessWithRunner` с подменой `ps`-runner в тестах; `List` стал read-only — он только фильтрует, без записи; добавлены `PruneStaleSession` (явная мутация running→exited + удаление каталога) и `RunningCount` (дешёвый probe); `KnowledgePanel.jobsChangedMsg` вызывает `PruneStaleSession` перед чтением списка. Регрессии: `TestPidIsSameProcessDeadPIDReturnsFalse`, `TestPidIsSameProcessKeepsLiveJobEvenWhenPSMissing`, `TestPidIsSameProcessDetectsRecycledPID`, `TestPidIsSameProcessAllowsSmallClockSkew`, `TestListDoesNotMutateDisk`, `TestPruneStaleSessionMarksDeadJobs`. Бинарник пересобран 18:25. Спека: `specs/robust-active-job-detection.md`.
[2026-08-01] Проблема: правая панель (KnowledgePanel) рисовала индивидуальные глифы для каждого действия (`~ thinking`, `R/W/G` + description, `>`, `X`, `A`, `i`), а левая (Tree) — единый `● word`; один и тот же статус читался по-разному в двух соседних местах → Решение: `actionIcon` в `internal/ai-knowledge/ui/render.go` теперь возвращает `● ` + каноническое слово для всех известных действий (`thinking`, `read`, `write`, `grep`, `find`, `analyze`, `wait`, `job`, `run`, `status`, `active`, `stop`→`stopped` для согласия с Tree), `idle` остаётся пустым (чтобы не дублировать `○ idle` из Tree), description вынесен из main-строки (он уже рендерится отдельной строкой через `pathStyle`); добавлены кейсы `find`/`wait`/`job`/`status`/`active` в `TestActionIcon`. `gofmt`/`go vet ./...`/`go test ./... -count=1` (включая e2e) зелёные, бинарник `./automata` пересобран. Спека: `specs/status-bullet-icon.md`.
[2026-08-02] Проблема: после `watch-jobs-panel` правая панель (KnowledgePanel) стала реактивной через fsnotify, а бейджи в левой (Tree) обновлялись из 5-секундного `RefreshKnowledgeCmd`, который в ходе `watch-jobs-panel` был удалён; `refreshTreeStatusBadges` (main.go) осталась мёртвым кодом, и Tree застряла на `○ idle` пока правая показывала актуальный статус → Решение: Tree-бейджи переведены на event-driven — `App.statusWatcher` (fsnotify) следит за `sessionBaseDir()`, `App.sessionWatchers` (map sessionID→fsnotify.Watcher) — за каждым видимым каталогом сессии, `treeStatusChangedMsg` из `watchTreeStatusCmd` вызывает `recomputeTreeStatusBadges` (переименована, без аргумента time.Time) и `syncSessionWatchers` (attach для новых чатов, close+delete для исчезнувших), re-arm через `statusWatchPending`. `Init` создаёт base через `MkdirAll` и поднимает watcher-ов; добавлен `Tree.Reset()` для тестов. Тесты: `TestRecomputeTreeStatusBadgesReadsAction`, `TestRecomputeTreeStatusBadgesIdleLeavesEmpty`, `TestSetupStatusWatcherCreatesMissingBase`, `TestSetupStatusWatcherAttachesPerSession`, `TestSyncSessionWatchersPrunesHidden`, `TestWatchTreeStatusCmdNilWithoutWatcher`, `TestTreeStatusChangedMsgTriggersRefresh`. `gofmt`/`go vet ./...`/`go test ./... -count=1` (включая e2e) зелёные, бинарник `./automata` пересобран 16:21. Спека: `specs/tree-status-watcher.md`.
[2026-08-03] Smoke-тест `TestSmokeStatusBadges` прошёлся по 13 экшенам с реальным `status.CachedReader` и поймал латентный баг: `Emoji("active")` и `Emoji("status")` падали в fallback `•`, хотя контракт функции (см. комментарий) — пустой глиф для действий без substatus. Tree всё равно показывал бы `○ idle` (потому что `statusEmojiToWord("•") → "" → fallback`), но это нарушало инвариант: действия, у которых в `Word` тоже `""`, должны иметь и в `Emoji` `""`. Корень — отсутствие кейсов в switch, которые `Word` уже покрывает. Решение: добавлены `case "active", "status": return ""` в `Emoji`; `weird` и другие неизвестные остаются `•` (для визуальной заметности опечаток в `status.json`); `TestEmoji` обновлён под новый контракт. Дополнительно: `TestSmokeStatusBadges` (smoke_test.go в корне) теперь постоянный регресс-тест на 13 экшенов + mtime invalidation + missing-file. Race detector на основных пакетах зелёный, `gofmt`/`go vet ./...`/`go test ./... -count=1` (включая e2e — `TestChatOpensWithPlanPane` флапнул один раз, повтор прошёл) зелёные, бинарник `./automata` пересобран 11:31.
[2026-08-04] Проблема: фоновая команда `sleep 60 &` в `job --command` создаёт orphan-процесс невидимый для KnowledgePanel и `job_list` — job extension запускает через `spawn($SHELL, ["-lc", command])`, записывает PID shell-обёртки, shell умирает в миллисекундах, `pidIsSameProcess` режет, `PruneStaleSession` удаляет `job.json`, а `sleep` живёт reparented к init. Аня запустила агент, в правой панели пусто. Решение: `analyzeSafety` в `~/.ai/just/pi/extensions/job/safety.ts` блокирует 7 категорий фоновых процессов — `cmd &` / `&!` / `&;` / `&|` / trailing `&` / subshell `(cmd &)`, плюс `nohup` / `disown` / `setsid` / `coproc` / `at` / `batch` через `BACKGROUND_DANGEROUS_PATTERNS`; `hasBackgroundOperator` использует `stripQuoted` чтобы не ловить `&` в кавычках. Добавлено 45 регресс-тестов в `safety.test.mjs` (позитивные/негативные/edge cases); typecheck + `node --test` зелёные. `spec.md` дополнен секцией AI Safety с ограничениями (`echo $((1 & 0))` блокируется ошибочно — задокументировано). Урок сохранён в `~/Space/AI/mistakes/job-extension-background-process-bypass.md`.
[2026-08-04] Проблема: после `watch-jobs-panel` KnowledgePanel стал реактивным, а ContextPanel (правая панель в FolderMode, показывает notes) — нет. Агент обновлял `notes.json`, в UI заметки появлялись только после перезапуска. Корень: в `context_panel.go` нет ни fsnotify, ни тика; `Refresh()` вызывается только из `SetDomain`, то есть при выборе другой папки и обратно. Решение: добавлен `notesWatcher *fsnotify.Watcher` + `notesWatchPending bool`; `setupNotesWatcher()` создаёт watcher на `domainDir(profile, domain)` с `MkdirAll` если каталога нет; `watchNotesCmd()` blocking Cmd возвращает `notesChangedMsg`; в `Update` обработка msg сбрасывает pending + вызывает `refresh()` + re-arm; `SetDomain` закрывает старый watcher перед созданием нового; `closeNotesWatcher()` для аккуратности. Тесты `TestContextPanelNotesWatcherReactive`, `TestContextPanelNotesWatcherClosesOnDomainChange`, `TestContextPanelNotesWatcherNilWhenNoDomain`. `gofmt`/`go vet ./...`/`go test ./... -count=1` (включая e2e) зелёные, бинарник `./automata` пересобран 19:40. Спека: `specs/context-panel-notes-watcher.md`. ChatMode (KnowledgePanel) notes НЕ показывает — by design, notes привязаны к домену, не к сессии.
[2026-08-13] Проблема: в Getic (`--pi getic`) и Synth (`--pi synth`) `× Clear` гасит чат (эмулятор убит), но JSONL не удаляется. Корень: `paths.SessionRoots()` возвращал **только** `~/.ai/just/pi/sessions/`, игнорируя `~/.ai/getic/pi/sessions/` (15 JSONL), `~/.ai/synth/pi/sessions/` (7 JSONL), `~/.ai/vexa/`, `~/.ai/weft/`, `~/.ai/ask/`. `FindSessionJSONL` искал только в just, fallback ничего не находил, `DeleteSessionJSONL` возвращал ошибку `session file not found` (но лог был пуст — скорее всего из-за `O_TRUNC` или запущенной старой сборки). Решение: `SessionRoots()` теперь сканирует `~/.ai/`, возвращает все существующие `<agent>/pi/sessions/`. Дедупликация через `seen`. Fallback на `just/pi/sessions/` если `~/.ai` пуст или недоступен. Регрессионные тесты `TestSessionRootsDiscoversAllAgents` (создаёт `just/getic/synth`, проверяет что все три в списке) и `TestFindSessionJSONLLocatesAcrossAgents` (JSONL в `getic/`, вызов с произвольным cwd, проверка что находится). Спека `specs/multi-pi-session-roots.md`. `go vet` чисто, `go test ./internal/...` зелёный, e2e `TestChatOpensWithPlanPane` флапнул один раз (известный кейс, повтор прошёл), бинарь `./automata` пересобран.

[2026-08-13] Регрессия безопасности после `multi-pi-session-roots`: расширенный `SessionRoots()` мог позволить профилю Getic найти и удалить сессию HumanHorizon при коллизии sessionID. Аня: «очень важно, чтобы учитывался параметр --pi». Решение: `FindSessionJSONL` и `DeleteSessionJSONL` получили третий обязательный параметр `agentDir string`; при непустом `agentDir` поиск только в `<agentDir>/sessions/`, при пустом — fallback на dynamic roots (для CLI/тестов). `DeleteSessionJSONL(sid, cwd, "")` возвращает ошибку `agentDir is required for safe deletion` — fail-safe. `clearSessionCmd` передаёт `a.piAgentDir`; если он пуст — логирует `refuse to clear %q without piAgentDir` и возвращает nil без удаления. `newTestApp` теперь устанавливает `piAgentDir` из `HOME/.ai/<profile>/pi`. Регрессионные тесты: `TestFindSessionJSONLRespectsAgentFilter` (just-сессия не видна через getic-фильтр), `TestDeleteSessionJSONLRefusesCrossAgent` (getic не удаляет just, файл остаётся), `TestDeleteSessionJSONLRefusesEmptyAgentDir` (пустой agentDir — ошибка), `TestClearSessionCmdRefusesWithoutPiAgentDir` (Clear с пустым agentDir возвращает nil, JSONL не тронут). Существующие тесты `sessionfile_test.go` обновлены под новую сигнатуру. Спека `specs/clear-session-agent-isolation.md`. `go vet` чисто, `go test ./...` зелёный (e2e флапнул один раз — известный кейс, повтор прошёл), бинарь `./automata` пересобран. Корень: `paths.SessionRoots()` возвращал **только** `~/.ai/just/pi/sessions/`, игнорируя `~/.ai/getic/pi/sessions/` (15 JSONL), `~/.ai/synth/pi/sessions/` (7 JSONL), `~/.ai/vexa/`, `~/.ai/weft/`, `~/.ai/ask/`. `FindSessionJSONL` искал только в just, fallback ничего не находил, `DeleteSessionJSONL` возвращал ошибку `session file not found` (но лог был пуст — скорее всего из-за `O_TRUNC` или запущенной старой сборки). Решение: `SessionRoots()` теперь сканирует `~/.ai/`, возвращает все существующие `<agent>/pi/sessions/`. Дедупликация через `seen`. Fallback на `just/pi/sessions/` если `~/.ai` пуст или недоступен. Регрессионные тесты `TestSessionRootsDiscoversAllAgents` (создаёт `just/getic/synth`, проверяет что все три в списке) и `TestFindSessionJSONLLocatesAcrossAgents` (JSONL в `getic/`, вызов с произвольным cwd, проверка что находится). Спека `specs/multi-pi-session-roots.md`. `go vet` чисто, `go test ./internal/...` зелёный, e2e `TestChatOpensWithPlanPane` флапнул один раз (известный кейс, повтор прошёл), бинарь `./automata` пересобран.
[2026-08-12] Диагностика Clear (открытый тикет `clear-session-replace-panel-em.md` остаётся отдельной задачей). `paths.FindSessionJSONL` для `humanhorizon__human-horizon.automata.ai` + `/Users/a/Space` возвращает корректный путь (`/Users/a/.ai/just/pi/sessions/--Users-a-Space--/2026-08-05T09-14-05-113Z_humanhorizon__human-horizon.automata.ai.jsonl`, 228 КБ), сам поиск работает. Однако `/tmp/automata-familiar.log` пуст, хотя `clearSessionCmd` всегда пишет `clearSession: deleted %s` или `clearSession: %v`. Это означает, что `clearSessionCmd` либо не вызывается (запущена старая сборка, где `cp.onClearSession == nil`), либо лог обнуляется на каждом старте (`O_TRUNC`). Следующий шаг: добавить временный `log.Printf("clearSession: ENTRY sid=%q cwd=%q")` на вход в `clearSessionCmd`, перезапустить, нажать Clear, посмотреть `/tmp/automata-familiar.log` — и только потом фиксить.
[2026-08-12] Удалены диагностические утилиты `cmd/check_clear/` (проверка FindSessionJSONL) и `cmd/fsdiag/` (тест внешнего fsnotify) — свою задачу выполнили (см. запись ниже). `go vet ./...` чисто, `go build -o automata .` успешен, smoke-тесты проходят.
[2026-08-13] Регрессия после `tree-status-watcher-session-events`: первый cmd-chain per-session watcher-а сработал, но не перезапустился — `case treeStatusChangedMsg` re-arm-ил только parent watcher через `watchTreeStatusCmd()`, для per-session этого не было; blocking read на `<-sw.Events` one-shot, и после первого события cmd-chain умирала. Tree-бейдж показывал только самое первое значение, дальше замирал на нём. Решение: добавлена `func (a *App) rearmSessionWatchers() []tea.Cmd` — возвращает cmd для всех ключей в `a.sessionWatchers`; вызывается из `case treeStatusChangedMsg` после `syncSessionWatchers()`. Per-session cmd-chain теперь живёт вечно (Init запускает, каждый event re-arm-ит). Регрессионный тест `TestPerSessionWatcherRearmsAfterEvent` фиксирует: первый event → cmd-chain завершается → re-arm cmd возвращается → второй event проходит. Спека `tree-status-watcher-session-events.md` дополнена секцией 1a (`rearmSessionWatchers`) и re-arm в Update. Все unit- и e2e-тесты зелёные, бинарь `./automata` пересобран 14:49. **Верифицировано на живом процессе 13.08: Аня перезапустила автомату, бейдж стабильно обновляется при записи в status.json (подтверждено вживую).**
[2026-08-12] Проблема: Tree-бейджи (`● run` / `○ idle`) перестали обновляться при изменениях `status.json` от агента. Корень — `tree-status-watcher.md` от 02.08 создал per-session `fsnotify.Watcher`-ы в `a.sessionWatchers[key]`, но никогда не запускал горутину-читатель `sw.Events`; parent watcher ловит только события на уровне `sessions/` (CREATE/REMOVE подкаталогов), а запись в `<sid>/status.json` проходит мимо. Доказательство: диагностические `DIAG:` логи (впоследствии удалены) показали `syncSessionWatchers visible=23 new_attached=21 attach_errors=0 total_watchers=21` — watcher-ы созданы — но `parent watcher event received` не появлялся после `WriteFile` в `status.json`. Внешний Go-тест через ту же библиотеку `fsnotify` на том же пути получал события нормально. Решение: добавлена `func (a *App) watchSessionCmd(key string) tea.Cmd` — blocking cmd-chain на `<-sw.Events` для каждого per-session watcher-а (аналог `watchTreeStatusCmd` для parent); `syncSessionWatchers` теперь возвращает `[]tea.Cmd` (cmd-ы только для новых watcher-ов, чтобы не дублировать); `Init` запускает их через `tea.Batch`; `case treeStatusChangedMsg` делает то же при новых чатах. Удалён вызов `syncSessionWatchers()` из `setupStatusWatcher` — per-session watcher-ы теперь прикрепляются только явным `syncSessionWatchers()` вызовом (Init или Update), чтобы возвращаемые cmd-ы попадали в bubbletea loop. Добавлены тесты `TestWatchSessionCmdNilWithoutWatcher`, `TestSyncSessionWatchersReturnsCmdsOnlyForNewWatchers`, `TestPerSessionWatcherCmdChainReceivesEvents` (регрессия: запись в `status.json` → `treeStatusChangedMsg` за <3с). Старые тесты `TestSetupStatusWatcherAttachesPerSession` / `TestSyncSessionWatchersPrunesHidden` / `TestPerSessionWatcherCmdChainReceivesEvents` обновлены: после `setupStatusWatcher()` теперь явно вызывается `_ = app.syncSessionWatchers()`. Спека: `specs/tree-status-watcher-session-events.md`. `go vet ./...` чисто, `go test ./... -count=1` зелёный (e2e `TestChatOpensWithPlanPane` флапнул один раз — известный кейс, повтор прошёл), бинарь `./automata` пересобран 21:40. Очистить `cmd/check_clear/` и `cmd/fsdiag/` (диагностические, не нужны в проекте) — спросить Аню.
[2026-08-04] Проблема: `internal/tree/status_badge_test.go:14` (`TestStatusBadgeFromMap`) делал `tr := New(); tr.Profile = "ai"; tr.AddChat("build").AddChat("docs")` — каждый вызов `AddChat` срабатывал `autoSave` и переписывал реальный `~/.ai/automata/profiles/ai/state.json`. Аня подтвердила: в её живом AI-профиле постоянно появлялись items `build` и `docs`. Аналогично мусорили ~40 тестов в `tree_test.go`, `substatus_test.go`, `input_test.go`, `main_view_test.go` — все с `tr := New()` без изоляции HOME писали в реальный профиль (через `Profile="" → "default"`). Решение: создан `internal/testutil/isolation.go` с `RunMain(m)`, который подменяет `HOME` и `AI_DATA_HOME` на `t.TempDir`-подобный каталог на время `m.Run()`, восстанавливает env после. В `internal/tree/testmain_test.go` вызов `testutil.RunMain(m)`. Покрывает весь пакет `tree` (главный источник засорения) — другие пакеты либо уже использовали `t.Setenv("AI_DATA_HOME", tmpDir)`, либо не пишут в profile. Реальный `state.json` Ани проверен — там только её дерево Vexa → AI, никаких `build`/`docs`. Регрессионный test pollution больше не возможен — даже если новый тест забудет `t.Setenv`, TestMain всё равно изолирует HOME.
[2026-09-06] Проблема: установленный TypeScript не поддерживает флаг `--ignoreConfig`, поэтому изолированная проверка extension завершилась с `TS5023` → Решение: использовать `pnpm exec tsc --noEmit --types node --skipLibCheck --allowJs index.ts`; тесты extension запускать отдельной командой `node --test index.test.mjs`.
[2026-09-06] Проблема: Go raw string fixture не может содержать обратные кавычки fenced Markdown, из-за чего `reader_test.go` не компилировался → Решение: хранить backticks в JSON fixture как `\u0060`, чтобы JSON-декодер восстановил их при чтении.
[2026-09-06] Проблема: первый вариант Markdown-рендерера объявил параметры `width`, `prefix` и `text` одним типом и сравнивал `rune` с `byte`, из-за чего UI не компилировался → Решение: явно типизировать `width int`, остальные параметры оставить `string`, а маркер привести к `rune`.
[2026-09-06] Проблема: ContextPanel fixture записывал `\\n` в raw JSON string, поэтому JSON содержал буквальные backslash-последовательности вместо переносов и Markdown-список не разбирался → Решение: в raw fixture использовать одинарные JSON escape-последовательности `\n`.
[2026-09-06] Проблема: notes были плоским массивом строк, API назывался `read_knowledge`, а ContextPanel показывал сырой текст без секций и Markdown → Решение: добавлены `NoteSection`/`sections` с legacy-нормализацией, канонический `read_notes` с alias `read_knowledge`, операции записи секций и встроенный Markdown-рендерер; `go vet`, полный `go test`, build, TypeScript typecheck и Node-тесты зелёные. Спека: `specs/knowledge-notes-sections-markdown.md`.
[2026-09-06] Проблема: промежуточная замена ContextPanel ссылалась на ещё не добавленные helpers/styles и старые тесты ожидали раскрытый контент → Решение: завершить layout/state helpers в одном UI-контракте и обновить тесты под collapsed-by-default с явным mouse/keyboard раскрытием.
[2026-09-06] Проблема: длинные Markdown-секции в Content занимали экран и не имели управления раскрытием → Решение: секции получили состояние collapsed-by-default, маркеры `▸`/`▾`, отступы, направляющую `│`, mouse/keyboard toggle, навигацию и сброс состояния при смене домена. Спека: `specs/context-notes-collapsible-sections.md`; `go vet`, полный `go test`, build и targeted diff check зелёные. `gofmt`/`go vet ./...`/`go test ./... -count=1` (включая e2e) зелёные, бинарник `./automata` пересобран 19:40.
[2026-09-06] Требования: переименование касается виртуальных папок, чатов и терминалов; реальный `BoundPath` не переименовывается. Переименование должно быть доступно через F2 и контекстное меню, конфликт имён должен завершаться ошибкой без объединения. Для папки нужно сохранить notes, планы, status, jobs, Kanban и chat JSONL; session ID пересчитывается. Перед переименованием папки останавливаются все рабочие чаты внутри неё, включая familiars. Для отдельного чата или терминала область остановки ограничивается выбранным элементом. В реализации остановленные процессы не запускаются автоматически; повторный выбор запускает их с новым ID.
[2026-09-06] Реализовано безопасное переименование дерева: preflight проверяет slug/session/domain-конфликты, затем останавливаются затронутые эмуляторы/jobs/familiars, переносятся профильные session directories, domain notes/Kanban и JSONL-заголовки, assignments Kanban обновляются на новые IDs, а UI/watchers получают новые IDs. `BoundPath` не изменяется. Спека: `specs/safe-tree-rename.md`; добавлены F2, контекстное меню, unit/integration/E2E-тесты. Правило: после изменения Automata обязательно пересобирать `./automata`, потому что E2E запускает бинарник, а не `go test`-пакет.
[2026-09-06] Проблема: попытка дописать тест через `write` перезаписала существующий `main_rename_test.go` → Решение: восстановить весь файл из актуального контекста, перед `write` проверять, создаётся ли новый файл; для дополнения существующего файла использовать точечный `edit`.
[2026-09-06] Проблема: новые тесты передавали `tea.KeyType` напрямую в `handleKey(tea.KeyMsg)`, из-за чего пакет tree не компилировался → Решение: в тестах явно создавать `tea.KeyMsg{Type: key}` через локальный helper; перед запуском проверять сигнатуры обработчиков клавиш.
[2026-09-06] Проблема: cuetty E2E не распознал первый вариант F1 только по `tea.KeyType`, поэтому help не открылся → Решение: для F1 поддержать и `msg.String() == "f1"`; адресные help/sort E2E после пересборки зелёные.
[2026-09-06] Проблема: добавление root-кнопки и изменение `create`-popover сдвинули меню на одну строку, из-за чего старые E2E создавали чат вместо терминала → Решение: сохранить прежний `Y=msg.Y` для create-popover; root-menu позиционируется отдельно. Адресные chat/terminal E2E после исправления зелёные.
[2026-09-06] Реализовано клавиатурное управление: `F5/F7` циклически переключают фокус, `F6` возвращает фокус дереву, `F1` открывает help, `F10` открывает контекстное/root-меню, стрелки/Home/End/PageUp/PageDown управляют деревом. В чате Ctrl/Alt/Tab/q/Ctrl+C больше не проходят через глобальные Warp shortcuts; они передаются активной панели. В заголовке добавлено root-меню `⋮`, внизу слева — `? Help`; root-сортировка по имени/типу сохраняет порядок в state.json. Спека: `specs/keyboard-navigation-and-root-sort.md`.
[2026-09-06] Проблема: широкий grep по личному `/Users/a/.ai/just/pi` захватил sessions и чувствительную конфигурацию → Решение: читать только явно разрешённые пути `themes/*.json`, `settings.json` и документацию; не искать по `sessions/`, `auth.json`, `models.json`.
[2026-09-06] Проблема: `pnpm exec tsc --noEmit` в just pi падает на двух существующих несовпадениях API в `extensions/dual/index.ts` → Решение: не менять несвязанный extension при задаче о теме; отдельно проверять JSON-конфигурацию и `pnpm exec pi --version`, а ограничение фиксировать явно.
[2026-09-06] Проблема: точечная замена конструктора `NewKnowledgePanel` добавила лишнюю закрывающую скобку и сломала синтаксис → Решение: перечитывать границы блока после замены, затем запускать `gofmt` и адресный тест; `go test ./internal/ui` после исправления зелёный.
[2026-09-06] Проблема: в `newApp` named return `a *App` был затенён через `a :=`, и сборка получила `no new variables on left side of :=` → Решение: использовать `a = &App{...}`; полный `go test ./...` после исправления зелёный.
[2026-09-06] Проблема: E2E-тест использовал отсутствующий метод `cue.Page.Click` → Решение: использовать `page.MouseClick(x, y)` и `page.Press`; E2E-пакет после исправления компилируется.
[2026-09-06] Проблема: фиксированная координата footer Settings не сработала в E2E из-за layout/границы Warp → Решение: вычислять строку и колонку `Settings` из `page.Lines()`; адресный E2E проходит.
[2026-09-06] Проблема: cuetty smoke с assertion через 500 мс проверял экран слишком рано → Решение: ждать минимум 2 секунды перед assertion; свежий бинарник показывает `? Help  Settings`.
[2026-09-06] Проблема: финальный `gofmt -l` обнаружил неотформатированный существующий `internal/ui/chat_panel_test.go` → Решение: применить `gofmt -w` и повторить `go test ./internal/ui`; пакет зелёный.
[2026-09-06] Проверки feature: `go vet ./...`, `go test ./... -count=1 -p 1`, `go build -o automata .` зелёные; новые unit/integration/E2E help/root-sort/focus тесты добавлены. Коммитов и перезапусков пользовательских процессов не делала.
[2026-09-06] Реализована система тем Automata: типизированный реестр `internal/theme` с первой темой `dinosaur-earth-sunny-grassy`, тема хранится в `state.json`, Settings рядом с Help открывает выбор и применяет его live. Палитра передана в Tree, Knowledge, Context, Kanban, Chat/Markdown и локальный Warp bridge (`Starframe/warp/theme.go`) для popover/modal/input/split controls. `just pi` получил тёпло-серые prompt/chat-фоны и розовые Markdown-ссылки. Спека: `specs/themes-and-settings.md`; финальные vet/test/build и cuetty smoke зелёные.
[2026-09-06] Уточнена палитра just pi по визуальной проверке: фон Main и custom/familiar сообщений унифицирован на тёмно-серой поверхности `bg1`; `thinkingHigh`/`thinkingXhigh`, `syntaxFunction` (включая названия папок) и `syntaxNumber` больше не используют фиолетовый или лавандовый blue, а используют серые токены. Фиолетовый оставлен только лёгким акцентом границы. Проверены JSON, Pi loader, extension tests, `pnpm exec pi --version`, полный Automata vet/test/build.
[2026-09-06] Проблема: Help и Settings открывались внутри Tree и занимали только левую панель, а `Ctrl+C` завершал Automata только при фокусе дерева → Решение: добавлены корневые App overlays поверх полного viewport, Tree callbacks для footer, root E2E-проверки и ранняя обработка `tea.KeyCtrlC`/`ctrl+c` до focus-router. `Alt`, `Tab`, `Shift+Tab` и остальные Ctrl-клавиши по-прежнему передаются активной панели. Спека: `specs/fullscreen-overlays-and-ctrl-c.md`; полный vet/test/build и cuetty smoke зелёные.
[2026-09-06] Изменено отображение Content: каждая заметка является одной collapsed-by-default секцией с единственным маркером `▸`/`▾`; внутренние `sections` объединяются при раскрытии, выводятся как Markdown-заголовки и больше не имеют отдельных интерактивных строк. Legacy notes, мышь, клавиатура, сброс домена и watcher сохранены. Спеки: `specs/context-notes-collapsible-sections.md`, `specs/knowledge-notes-sections-markdown.md`.
[2026-09-06] Сокращены pink/purple акценты Automata: папки, пути, заголовки, заметки, статусы, кнопки и активные вкладки используют серые токены; pink оставлен для Markdown-ссылок, purple — только для редкого Warp focus accent. Добавлены assertions для theme registry, Tree, native UI и Knowledge renderer. Спека: `specs/themes-and-settings.md`.
[2026-09-06] Проблема: палитровый тест сначала обратился к `newRenderStyles` из другого Go-пакета → Решение: перенести Knowledge assertion в `internal/ai-knowledge/ui/render_test.go`, а UI assertion оставить только для стилей пакета `internal/ui`.
[2026-09-06] Проблема: единичный прогон полного `TestContextPanel` получил несвоевременно закрытый fsnotify Events channel → Решение: повторный адресный прогон изменённых Content-тестов и пять прогонов watcher-теста зелёные; код закрытия watcher не менялся.
[2026-09-06] Проблема: общий `git diff --check` нашёл trailing whitespace в сгенерированных `e2e/test-artifacts/*.render.txt` из-за терминального padding → Решение: проверять targeted diff исходников и спецификаций отдельно; релевантный diff чистый, артефакты не редактировать.
[2026-09-06] Исправлены отступы Content: заголовок заметки начинается с одного пробела, а `│` раскрытого блока находится в той же колонке, что и `▸`/`▾`. Добавлены точные assertions геометрии. Спека: `specs/context-note-indentation.md`.
[2026-09-06] Проблема: тест попытался вызвать отсутствующий `lipgloss.StripAnsi` → Решение: использовать `github.com/charmbracelet/x/ansi.Strip`; адресные тесты отступов зелёные.
[2026-09-06] Уточнён отступ раскрытого контента: после линии `│` ровно два пробела до текста; префикс `renderMarkdownBlockWithTheme` теперь ` │` без дополнительного пробела.
[2026-09-08] Проблема: после удаления локального `status.slugify` status-тесты временно падали с `undefined: slugify`, а дублированный `dataHome`/`slugify` status-root расходился с canonical default/Unicode paths → Решение: удалить устаревший `TestSlugify`, перевести status readers на `paths.SessionDir` и добавить canonical default/Unicode tests.
[2026-09-08] Проблема: первый вызов `gofmt` получил список одобренных файлов одной строкой и вывел `lstat ...: no such file or directory` → Решение: передавать пути отдельными аргументами; повторные `gofmt` и targeted tests прошли.
[2026-09-08] Проблема: первый canonical status cuTTY smoke искал полный статус `● thinking`, но узкая Tree-колонка показывала обрезанное `● thinkin` → Решение: проверять фактически видимую строку или увеличить ширину; повторный smoke с bounded assertion прошёл 2/2.
[2026-09-08] Проблема: финальный `gofmt -l` получил несколько путей одной строкой и завершился с ошибкой (exit 1) → Решение: передавать каждый путь отдельным аргументом; повторная scoped-проверка завершилась успешно.
[2026-09-08] Проверено: domain-path fix использует `paths.DomainDir`/`effectiveProfile`; memory/UI tests покрывают default, `AI_PROFILE` и `Проект Ω`, а targeted/full test, vet, build и scoped diff-check завершились успешно.
[2026-09-08] Удалены безусловные синхронные записи mouse events в `/tmp/context-mouse.log` и `/tmp/container-mouse.log`; маршрутизация, `tea.WithMouseAllMotion` и watcher error logging сохранены. Добавлен статический regression-тест `TestMouseHandlersDoNotWriteFixedDiagnostics`; существующие `/tmp`-файлы не удаляются и не переносятся.
[2026-09-08] Реализована граница хранения Dual Task ID: единый basename-валидатор и containment внутри `dual/tasks` выполняются до task-file I/O; generated/legacy IDs сохранены, unsafe separators/NUL/dot/traversal/absolute IDs и invalid dependencies возвращают structured `invalid_task_id`, безопасный отсутствующий ID — `unknown_task` без state side effects. Добавлены изолированные regressions для всех affected lookup paths и scheduler. Спека `specs/dual-task-id-storage-boundary.md` закрыта; job/familiar/session/workspace и message/coordinate association не изменялись.
[2026-09-08] Ошибка процесса: для поиска Automata `CONTEXT.md` был запущен слишком широкий `find /Users/a`; команда была остановлена. Правило: при scoped-задаче использовать только заранее известные абсолютные пути и не повторять широкие обходы.
[2026-09-08] Dual task ID storage boundary реализован в `just/pi/extensions/dual/index.ts`: Task ID проверяется как безопасный basename до task-file I/O, кандидат подтверждается containment внутри `dual/tasks`, а invalid/unknown и dependency failures возвращают structured `invalid_task_id`/`unknown_task` без частичных записей. Добавлены изолированные traversal/NUL/separator/legacy/dependency regressions в `extensions/dual/index.test.mjs`; затронутые message/coordinate associations, job/familiar/session/workspace paths и UI/F5/F7 не менялись. Targeted Dual: 40/40, полный Node-набор: 115/115, `tsc --noEmit`, Pi 0.75.5 и scoped diff-check зелёные; коммит не создавался.
[2026-09-08] Проблема: безопасные Task ID boundary paths должны сохранять исходный basename без trim/normalization, а invalid dependency или неизвестная задача не должны запускать последующие writes → Решение: общий validator/read/write result boundary теперь передаёт исходные IDs, все affected tools и scheduler возвращают structured errors до mutation, а generic message/coordinate associations оставлены совместимыми. Повторно подтверждены Dual 40/40, полный Node-набор 115/115, `tsc --noEmit` и Pi 0.75.5.
[2026-09-08] Проверка: первый изолированный symlink-harness (`job_f6e2...`, 01:05:50) завершился успешно и подтвердил symlink escape; второй confirmation harness (`job_41f2...`, 01:07:04) завершился ошибкой только из-за отчёта, который ссылался на неопределённую `outsideFileChanged`, тогда как локальная переменная называлась `outsideChanged` → Решение: не использовать broken ad-hoc harness; правило — перед запуском inline harness сверять имена переменных с полями отчёта, а focused symlink-тест считать авторитетным: он проверяет structured rejection и отсутствие внешнего read/write.
[2026-09-08] Проблема: лексический containment не останавливал symlink-кандидат или symlink каталога `dual/tasks`, который мог перенаправить task-file I/O наружу → Решение: `taskFile` отклоняет существующие symlink через `lstat` до чтения/записи; тесты проверяют безопасный candidate ID, inspect/update и неизменность внешнего файла.
[2026-09-08] Проблема: определённый non-array `dependencies` мог быть принят scheduler за пустой список, а существующий symlink `dual/tasks` или candidate мог перенаправить task-file I/O наружу → Решение: defined non-array dependency container возвращает structured `invalid_task_id` и не диспатчится; центральный `taskFile` через `lstat` отклоняет существующий tasks-directory/candidate symlink до чтения или записи. Hard links, session IDs и profile IDs не проверяются и не изменяются; добавлены изолированные regressions для обеих границ.
[2026-09-09] Проблема: dispatch/schedule/review_task могли оставить familiar envelope или bookkeeping после ошибки записи Task → Решение: общий typed commit helper записывает envelope и подготовленный Task snapshot, при обычном I/O откатывает точный envelope/Task и возвращает `transaction_failed` с phase/residual state; notifications, audit signals и scheduler bookkeeping происходят только после commit. Crash/SIGKILL, power loss, rollback failure и TOCTOU между независимыми файлами остаются явно ограниченной границей; новые transport/daemon/store не добавлялись.
[2026-09-09] Проблема: Dual cancel/inspect и глобальные job lookup могли пересекать foreign profile/session при одинаковых или связанных job/task IDs, раскрывать metadata и останавливать чужой процесс → Решение: добавлены typed JobScope/profile-session lookup APIs, validateJobId до filesystem I/O и повторная provenance-проверка в stopJob перед SIGTERM/write; Dual inspect/cancel ограничены firm scope, foreign JobRecord удаляются из ответов/ошибок, reviewer denial и race/traversal покрыты regressions; SIGTERM, lifecycle/inbox, job.json schema и локальные job tools сохранены.
[2026-09-09] Проблема: рассинхронизация имени Dual job API между import и вызовом (`getJob`/`listJobsForId`) вызвала ReferenceError → Решение: сверять экспорт, import и call-site и запускать targeted Dual tests после правки.
[2026-09-09] Проблема: scoped stop/lifecycle writes могли использовать изменившийся profile/inbox из окружения → Решение: захватывать exact profile/inbox scope на входе и сохранять explicit null; unscoped путь не менять.
[2026-09-09] Проблема: finalize с fallback-профилем не находил jobs при multi-profile cleanup → Решение: cleanup передаёт фактический profile как explicit scope, а refresh/child finalize без override используют legacy global readJob.
[2026-09-09] Проблема: `git diff --check ...` сначала запускался из `/Users/a/Space`, не являвшегося git-репозиторием → Решение: выполнять scoped diff-check из cwd репозитория `/Users/a/.ai/just/pi`.
[2026-09-09] Проблема: scoped pause-spec diff-check был запущен из `/Users/a/Space` вне git-репозитория и завершился exit 129 → Решение: запускать scoped diff-check из `/Users/a/.ai/just/pi` или указывать корректный каталог репозитория.
[2026-09-09] Проблема: persisted Task.initiator мог быть небезопасным или foreign и направлять dependent I/O за пределы canonical firm session → Решение: добавлены FirmSessionRef/provenance validation, safe invalid_task_record/unknown_task rejection, profile/session isolation, scheduler/inspect/mutation guards и regressions; hard links/ancestor symlinks/TOCTOU остаются отдельным scope.
[2026-09-09] Проблема: Task provenance проверки должны использовать один захваченный base/profile/session scope во всех dependent I/O → Решение: Dual передаёт FirmSessionRef в readers, writers, transactions, jobs, signals, subscriptions и workspace inspection, не выводя storage root из persisted initiator.
[2026-09-09] Проблема: familiar job tools использовали global lookup и могли раскрывать или останавливать foreign jobs → Решение: job_status/job_logs/job_stop/job_list используют canonical profile/session scope, safe renderer и provenance recheck; foreign same-ID jobs дают not_found без metadata, writes, logs, inbox или SIGTERM.
[2026-09-09] Проблема: Task workspace/scope не ограничивали familiar write/edit и job workdir/task association → Решение: current familiar Task разрешается только через parent firm registry и ровно один running Task; direct write/edit проверяют lexical workspace + non-empty scope, а job проверяет workdir/task_id до status/job directory/spawn. Shell sandbox, symlink/hard-link и TOCTOU остаются отдельным residual scope.
[2026-09-09] Реализован необязательный Dual-инструмент `pause({ reason?: string })`: он возвращает ровно `{ status: "paused" }`, ставит максимум одну `queueMicrotask(() => ctx.abort())` за текущий turn и сбрасывает one-shot guard на `agent_end`; ожидание, I/O, storage, inbox, jobs, Tasks, familiars и alias `yield` не добавлялись. Добавлен детерминированный regression с проверкой Director whitelist, результата, reset между turns и отсутствия файловых побочных действий.
[2026-09-09] Проблема: составная zsh-проверка присвоила exit code переменной `status`, которая является readonly special parameter → Решение: использовать имя `exit_code` и отдельно нормализовать ожидаемый exit 1 от `git diff --no-index` для untracked spec; правило записано в `AI/mistakes/2026-09-09-zsh-readonly-status-variable.md`.
[2026-09-09] Проблема: Task storage проверял lexical path и symlink только у `tasks`/candidate, оставляя ancestor-symlink, hard-link candidate и замену объекта до I/O → Решение: canonical ancestor chain fail-closed, повторная проверка перед open, `O_NOFOLLOW` descriptor read/write с `fstat` `nlink === 1`, exclusive create без EEXIST fallback, identity recheck для rollback/delete и deterministic regressions; последняя race после `unlink` recheck остаётся явно ограниченным residual из-за отсутствия переносимого Node `unlinkat`.
[2026-09-09] Проблема: create-race regression для `assign` привязалась к вымышленному Task ID, хотя production `createTask` генерирует случайный ID → Решение: hook сопоставляется с любым candidate внутри canonical `tasks` directory и проверяет фактически перехваченный путь; правило записано в `AI/mistakes/2026-09-09-dual-create-race-test-random-task-id.md`.
[2026-09-09] Проблема: create-race regression для `assign` ожидала `transaction_failed`, хотя `assign` возвращает прямой `writeTask` error без transaction wrapper → Решение: проверять `invalid_task_id` и отсутствие перезаписи candidate; правило записано в `AI/mistakes/2026-09-09-dual-assign-fail-closed-error-shape.md`.
[2026-09-09] Проблема: scoped `git status` был запущен с `/Users/a/Space`, который не является репозиторием → Решение: использовать только подтверждённые repository roots Pi/Automata, а `AI/mistakes` проверять как обычные файлы; правило записано в `AI/mistakes/2026-09-09-git-status-space-not-repository.md`.
[2026-09-09] Проблема: `job` не отличал явно объявленное production-действие и мог запускать его без human confirmation → Решение: добавлен opt-in `production: true` с существующим `ctx.ui.confirm` до cleanup/storage/spawn; отказ, отсутствующий UI и невалидный flag fail-closed, обычный job path сохранён.
[2026-09-09] Проблема: malformed edit временно добавил служебный Unicode-мусор в строку job ID → Решение: перечитать точный repr строки, удалить только мусор, запустить job suite и `tsc`; правило записано в `AI/mistakes/2026-09-09-job-production-confirmation-edit-payload.md`.
[2026-09-09] Shell boundary подтверждена regression-тестом: job extension удаляет built-in `bash` из active tools на `before_agent_start`; полноценный OS/container sandbox и `user_bash` не заявляются без отдельного контракта.
[2026-09-10] Проблема: `routeCachedEmulatorMessage` маршрутизировал PTY только через `emulatorCache`, а повторное создание familiar заменяло работающий эмулятор → Решение: маршрутизация проверяет оба кэша, напрямую продолжает `Listen` найденного emulator, а `createFamiliarEmulator` возвращает существующий кэшированный экземпляр; Close/Clear не изменены.
[2026-09-10] Проблема: первый полный `go test ./...` нестабильно не дождался обновления экрана в `e2e/TestRenameChatWithF2`, оставив `Old Chat` → Решение: повторить изолированный E2E-тест после завершения предыдущего запуска; повторный запуск прошёл, production-код и тест переименования не менялись.
[2026-09-10] Проблема: повторный полный `go test ./...` нестабильно не дождался модалки `Chat name` в `e2e/TestChatOpensWithPlanPane` → Решение: повторить изолированный E2E-тест с тем же свежим executable; повторный запуск прошёл, production-код и E2E-тест не менялись.
[2026-09-10] Проблема: после `PtyExitMsg` familiar-кэш сохранял эмулятор с завершившимся PTY, и возврат в чат мог переиспользовать мёртвый экземпляр → Решение: удалять familiar из `familiarEmulatorCache` и `activeSessions`, а `PtyExitMsg` оставлять активному `ChatPanel` для удаления вкладки и polling; следующий `createFamiliarEmulator` создаёт новый экземпляр.
[2026-09-10] Проблема: полный `go test ./...` нестабильно измерил недостаточный рост ChatPanel после collapse в `e2e/TestChatResizesOnTreeCollapse` → Решение: считать это timing-зависимым E2E-сбоем при параллельной нагрузке и проверять изолированным rerun; production lifecycle-код не менялся.
[2026-09-10] Проблема: preexisting-modified tracked executable `automata` был перезаписан через `git show HEAD:automata > automata`, а точная исходная копия в этом случае недоступна → Решение: нельзя восстанавливать или перезаписывать такой файл через эту команду; перед любым build нужно сохранить исходный файл или не трогать его, поэтому точное восстановление невозможно.
[2026-09-10] Проблема: при поэтапном добавлении clipboard-кода импорт `encoding/json` временно не использовался, поэтому targeted UI-тест не собрался → Решение: после каждого промежуточного изменения сначала завершать связанный код или откатывать неиспользуемый импорт; перед следующим шагом запускать `gofmt` и адресный тест.
[2026-09-10] Реализованы кнопки `Copy`, `Paste +` и `Paste replace` в Content-панели: все заметки копируются в JSON с секциями, вставка валидируется и добавляет либо атомарно заменяет `notes.json`, системный clipboard изолирован интерфейсом и UI обновляется без перезапуска. Добавлена спецификация `specs/context-notes-clipboard.md`; коммит не создавался.
[2026-09-10] Проблема: исходники содержали toolbar заметок, но запускаемый tracked-бинарник `automata` был собран до этой функции, поэтому кнопки не отображались → Решение: пересобран `/Users/a/Space/Projects/HumanHorizon/automata/automata`; уже запущенные процессы должны быть перезапущены.
[2026-09-10] Проблема: финальный общий `go test ./...` снова получил timing-сбой `e2e/TestChatOpensWithPlanPane` (`Chat name` не открылся за 2.84s) → Решение: изолированный повтор `go test ./e2e -run '^TestChatOpensWithPlanPane$' -count=1 -v` прошёл; считать это известным E2E-флапом, не связанным с toolbar заметок.
[2026-09-10] Проблема: вставка заметок могла сразу читать clipboard и менять `notes.json`, а toolbar был без вертикального пространства → Решение: перед `Paste +`/`Paste replace` добавлено модальное подтверждение с отменой через `No`/`N`/`Esc`/крестик/внешний клик, а toolbar размещён между двумя пустыми строками; добавлены regression-тесты.
[2026-09-22] Проблема: два полных E2E-прогона получили stale PTY/terminal fixture в `e2e` (`TestRootChatSessionID`, `TestFolderChatSessionID`, `TestUniqueSessionID`) → Решение: production-код не изменять; завершить предыдущий cuTTY/тестовый процесс и повторить suite изолированно перед коммитом.
[2026-09-22] Проблема: session-ID E2E задавал `PI_CMD=/bin/bash`, а общий resolver корректно передаёт Pi аргументы `--session-id`, поэтому Bash завершался с ошибкой `invalid option` → Решение: тест использует временный fake-Pi wrapper, который удаляет только эти аргументы и запускает Bash; runtime-контракт `PI_CMD` не ослаблен.
